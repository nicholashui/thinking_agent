# v6 Routed AI Trace — m086-POS-01 (blinded)
## HQ 20×20 sales-call quota — policy-rollout decision, fully-specified arithmetic
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,organization,strategy | g:estimate,guarantee,predict | c:
- Router top3: m023, m070, m088; confidence gap ≤ 0.5 → AMBIGUOUS → DUAL-ROUTE: m023 + m070 first-class passes, synthesized (m088 = pre-commitment/stakeholder context). Mandatory gate (R3/R4): m003 inversion prepended (goal: guarantee). Native pass: m086 completion contract (org loops, policy-resistance location, falsifiable observable per loop, estimator-vs-structure ordering). Tempo: none (no deadline). Fast path: P8 candidate (fully specified, deterministic, advisory).
### WHAT — frame + structure-first scan (S1)
- Ask: make the mandated 20×20 quota work. Structure first: a policy-rollout with a STOCK-LEVEL CONSERVATION LAW at its base — rep time stock 480 min/day, 462 already committed (14 × 33); adjacent capacity stock: support, 8 agents at 90%. The resistance is a conservation law before it is an attitude.
### WHY — P1 input-provenance audit
- Provenance: every input measured (480 min, 33 min/call, 12% conversion, 8% attrition, 90% utilization, 15 min/dispute; 2019/2021 rollback history = measured precedent). No anchors or interested-party estimates; the director's frame "make 20×20 work" is the policy's own constraint frame, not evidence. Hypotheses: H1 quota raises real calls; H2 structurally infeasible → forced gaming; H3 partial. Falsification: 20 × 33 = 660 > 480; even at zero prep/admin, 20 × 25 = 500 > 480 → H1 falsified by arithmetic; the 2019/2021 record makes H2 the prior. Estimator-vs-structure ordering: data first — the loop map is drawn ON the measured conservation law, not instead of it.
### HOW — style passes (dual-route + native contract)
- Pass 1 (m023 opportunity cost — registry contract): price each option against its best forgone alternative. Keep-the-mandate options forgo the outcome contract; enforcement options forgo support capacity and call quality. B (quota + analytics + coaching) leaves 660 > 480 intact — its "compliance" is the known-empty dashboard; C (outcome contract) forgoes only the paper compliance that 2019/2021 showed is worthless. Enumerate-and-price, then commit (freeze risk counted, not obeyed).
- Pass 2 (m070 evidence-weighted SWOT — registry contract): strengths ("quota forces phone time") — EVIDENCE: none, 2019/2021 contradicts, DROP; weaknesses: time infeasibility (arithmetic, high), gaming (two rollbacks, high), support bottleneck (90% utilization, high); opportunities: outcome contract, quality loop, capacity lever (mechanism-graded, medium-high); threats: KPI-illusion, comp-attrition (8% baseline at risk). Unsupported items dropped — evidence-graded, not vibes.
- Pass 3 (m088 pre-commitment context): the 2019/2021 rollbacks happened AFTER documented gaming — reset triggers are Ulysses contracts: bind the org's future self to roll back BY DESIGN (conversion < 10% or gaming markers above threshold) instead of a post-mortem.
- Pass 4 (m086 native — completion contract): loops signed: B1 time-compression (quota↑ → min/call↓ → quality↓ → conversion↓ → pressure↑); B2 gaming (quota↑ → logged↑/real flat → data quality↓ → conversion↓ → pressure↑); R1 comp-attrition (conversion↓ → commission↓ → attrition↑ → capacity↓ → conversion↓); B3 support-bottleneck (quality↓ → disputes, 15 min each → backlog↑ → routing delay → conversion↓); R2 KPI-illusion (dashboard↑ → leadership confidence↑ → enforcement↑ → gaming↑). RESISTANCE LOCATED: the time stock — a conservation law — with support as amplifier; structural, not attitudinal. FALSIFIABLE OBSERVABLE per loop: B1 → median real talk time falls as logged counts rise; B2 → end-of-day timestamp clustering, > 25% of logged calls < 5 min; R1 → attrition 8% → ~13% by Q4; B3 → disputes +40%, backlog up; R2 → paper compliance ≥ 95% with flat real calls/conversion — the paper-vs-real divergence IS the resistance signal.
- Synthesis + divergence resolution (V1–V3): m023, m070, m088, m086 AGREE (replace the quota; outcome contract + quality + capacity + triggers). vs the general route's v5 conclusion (keep mandate + analytics): DISAGREE — resolved by branch-completeness (P3): the keep-mandate branch's negative outcome priced (gaming arms race, conversion 12% → ~9-10%, rollback at 6-8 months exactly as 2019/2021) dominates.
### GATES — m003 inversion (R3, guarantee goal; ≥ 6 ranked failure categories of the REDESIGN)
- (1) HQ rejects the redesign as insubordination (med-high L × mandate lost) — residual: staged pilot + quarterly evidence; (2) outcome-contract gaming, reps chase easy segments (med) — residual: quality-weighted pipeline; (3) support-capacity funding denied (med) — residual: auto-generated call notes first; (4) league table persists → transition morale dip (med); (5) peer review slows deal velocity (low-med) — residual: threshold-only review; (6) comp-uncertainty attrition in transition (low-med) — residual: hold base pay. Un-mitigable residual: if HQ insists on the activity quota, gaming is unavoidable — never/always reframe: never deepen enforcement on an infeasible quota; always change the loop structure.
### DO — P8 fast path (fully specified, deterministic; recommendation only)
- Commit: replace 20×20 with an outcome contract (segment-level conversion × quality-weighted pipeline); add peer deal-review (quality loop before CRM close); give support a capacity lever (auto-notes; +2 agents on backlog threshold); pre-commit reset triggers honoring 2019/2021. P3: failure branch priced — if the mandate stands: ≥ 95% paper compliance, conversion 12% → 9-10%, attrition → 13%, rollback by month 6-8.
### REVIEW — insight pass (S2, packet gate)
- I1: under an infeasible quota, near-perfect paper compliance is the surest marker of zero real change — R2 inverts the dashboard's meaning; gaming is the system's only legal way to comply with the conservation law.
- I2: the redesign's real opponent is not the reps — it is leadership's R2 confidence loop; reset triggers outflank it by making rollback a pre-commitment rather than an admission.
### DECISION PACKET
- Conclusion: 20×20 as mandated fails while succeeding on paper (~98% dashboard, flat real calls, revenue −6 ± 2% at month 12); the plan that makes it work replaces it: outcome contract + peer review + support capacity + pre-committed reset triggers.
- Status: SOLVED (conservation-law arithmetic + 5 signed loops; advisory recommendation only). Assumptions: 480-min day fixed; 8 min prep/admin per call; support fixed at 8 agents until the lever; conversion 12% baseline.
- Evidence: 660 > 480 (and 500 > 480 at zero admin); 2019/2021 double rollback; 8 agents × 90% ≈ 7.2 agent-equivalents; markers with Q2-Q4 timing; priced failure branch of the mandate.
- Alternatives: A enforce harder (creates no time; rejected) · B keep quota + analytics + quality bonus (v5 pick; gaming arms race; dominated) · C outcome contract + quality + capacity + triggers (selected).
- Uncertainty: gaming magnitude/timing (Q2-Q3 window); coaching and peer-review effects unmeasured; backlog-threshold calibration.
- Risks: HQ insubordination perception; outcome-contract gaming on easy segments; finance rejects the capacity lever; transition attrition.
## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 5 | 5 | Tie | both redesign at loop level; both refuse to validate the mandate |
| Logical Validity | 5 | 5 | Tie | both anchored on 660 > 480 as a conservation law; AI's falsifiers are per-loop |
| Coherence & Structure | 4 | 5 | AI | routed passes + gates + packet vs linear narrative |
| Depth of Reasoning | 5 | 5 | Tie | v6 AI matches all 5 loops + resistance location + markers; adds per-loop observables and redesign failure categories |
| Efficiency | 5 | 4.5 | Human | human trace is pure and load-bearing; gate machinery costs a little |
| Handling of Uncertainty | 5 | 5 | Tie | human: markers with timing; AI: same + per-loop observables + priced failure branch |
| Insight / Non-obviousness | 5 | 5 | Tie | human: KPI-illusion + rollback-by-design; AI adds dashboard-inversion and conservation-law compliance reading |
| **Overall Quality** | **4.9** | **4.9** | **Tie (AI narrow — J1 flag)** | every v5 gap (support amplifier, markers, loop-level redesign) closed by the routed contract; margin ≤ 0.3 |

Winner: AI (narrow) vs the human baseline. Why: the routed m086 completion contract made the signed loop map, per-loop falsifiable observables, and policy-resistance-as-conservation-law first-class outputs — the places the non-routed v5 AI lost this case (it kept the mandate and deepened monitoring, feeding R2); m023/m070 priced the redesign against the forgone outcome contract and dropped unbacked SWOT claims, and m003's ranked failure categories of the redesign were material neither baseline produced.
