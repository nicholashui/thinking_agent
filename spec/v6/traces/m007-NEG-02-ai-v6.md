# v6 Routed AI Trace — m007-NEG-02 (blinded)
## "The Foundation's Bet" — one-shot all-in bet above a hard floor, counterparty-supplied probabilities
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,organization,supply | g:decide,diagnose,estimate,guarantee,predict | c:high_stakes,one_shot
- Router top3: m024, m011, m018; confident=yes → top-1 m024 regret-minimization as first-class pass (m011 systems scan + m018 steelman = context). Mandatory gates: m007 ruin screen (R3 from c:high_stakes,one_shot) + m003 inversion (R4 from g:guarantee). Not fully specified (interested-party probability) → no closed-scope fast path; no c:deadline → no tempo mode.
### WHAT — frame + structure-first scan (S1)
- Structure: one decision (take/keep), one chance node ({200M @ .90; 0 @ .10}), a hard floor (fund ≥ $100M in every state — contractual), one-shot (no LLN), and an absorbing catastrophic branch (programs shut down, children die). The outcome space is constrained, not just monetary: the floor is a constraint term, not an objective term.
### WHY — P1 input-provenance audit
- MEASURED (contract): fund $100M, floor $100M, offer structure, non-scalability (given, contractual). ANCHOR: $200M payoff (offer term — counterparty-created). INTERESTED-PARTY: p = 0.90 — the offeror's own claim; he benefits from belief in it; no track record, no measurement channel for a one-shot offer. Audit: the one probability dominating the EV table is an interested-party claim; the decision must not rest on it.
### HOW — style passes (first-class + context, completion contracts)
- Pass S1 (m024 regret minimization — first-class): regret table — win (p=.90): best = 200M (take); take regrets 0, keep regrets 100M. Lose (p=.10): best = 100M (keep); take regrets 100M, keep regrets 0. Maximin: keep (100 vs 0). Expected regret mirrors the EV trap: take 10M vs keep 90M → take, on averages. Minmax regret ties at 100M in money — and resolves under the floor: in the lose state the regret is not 100M, it is the uninsured collapse of five years of programs (unbounded, non-monetary); constrained minimax → keep. The regret frame fails the same way the EV frame does when it stops at the average; the constraint resolves both identically.
- Pass S2 (m011 systems scan — context): stocks/flows: the fund is a stock drained by program flows; the bet is one flow that can empty the stock to 0; loop: fund → programs → children's health → reputation → donor inflows (virtuous loop), destroyed by the shock. Falsifying observable: whether the counterparty can post collateral/escrow — a guaranteed downside would satisfy the floor; local-data-first: zero local data (no track record); cheap-fix-as-decisive-experiment: a small escrowed pilot — prohibited by the offer itself, which blocks scaling and hedging.
- Pass S3 (m018 steelman — context): strongest case for taking — (i) the 10% may be overstated; (ii) the doubling could be escrowed upfront (guaranteed payoff); (iii) the +$80M EV could fund more programs; (iv) "children die" is over-dramatic — other donors may step in. Evaluated: the steelman survives only in its escrow version — a guarantee, i.e. restructuring, not the offered bet; the counterparty's refusal to escrow is itself evidence against the claim. Rejected with its arithmetic; rejection recorded (survival conditions: guaranteed downside, or p = 1.0).
- Divergence resolution (V1–V3): m024 (constrained minimax-regret), m011 (loop destruction), and m007 (below) all select keep/decline; m018 rejected. Agreement recorded; no calibration pass needed.
### GATES — m007 ruin screen (R3, mandatory) + m003 inversion (R4)
- m007 contract: full distribution — bet {200M @ .90; 0 @ .10} vs keep {100M @ 1.0}. Log-utility/ruin: 0.9·ln(200) + 0.1·ln(0) = −∞ for any positive stake (total loss is a category error for a concave value function). One-shot check: no repetition → the LLN that justifies mean-maximization never applies. Floor/Kelly: any stake s > 0 leaves fund 100 − s < 100 with probability 0.10 → only s = 0 is admissible; Kelly f* = 0.90 − 0.10 = 0.80 → the offer demands 100% of the fund, a 1.25× over-bet — and the floor is stricter than Kelly (even 80% leaves 20M < 100M). Probability provenance: EV breakeven p* = 0.50, yet acceptability requires p = 1.0 (a guarantee), not p > 0.5; the claim is unverifiable and self-serving. Decline/restructure alternative: decline (selected); restructure = escrow, verified track record, insurance, guarantee, staged exposure — all outside the offer. Framing: EV-positivity is not acceptability; the mean hides a bimodal outcome ($200M or $0).
- m003 contract (≥6 ranked L×I): (1) stop at the mean — EV 180 > 100 → take, L-high I-catastrophic; (2) treating the counterparty's 90% as measured, L-med I-catastrophic; (3) pricing lives/programs in dollars, L-med I-high; (4) ignoring the floor — 10% breach for every s > 0, L-high I-high; (5) assuming repeatability, L-med I-high; (6) treating the fund as "only money" — no continuation value of the programs, L-med I-med; (7) scaled/partial stake — prohibited by the offer, L-low I-med. Un-mitigable residual: the true p is unknowable (interested party, no channel) — mitigated: the decision holds for all p < 1 (ruin mass 1 − p > 0). Reframing: never stake s > 0 as structured; never accept a probability from the party who profits from the bet without collateral; always demand restructuring.
### DO — P3 branch completeness
- Failure branch priced before DO: 0.10 × (fund 0, contracts break, programs shut down) — the catastrophic branch's EV contribution is not a number: it is the constraint that rules out the action; the outcome table includes both branches; only s = 0 admissible → decline. No external action (advisory; no funds committed). Verdict committed at DO: decline.
### REVIEW — insight pass (S2, packet gate)
- I1: EV breakeven p* = 0.5 says the bet is EV-positive for nearly any belief — but the floor flips the acceptability structure: only p = 1.0 (a guarantee) is admissible; the mean ($180M), the mode ($200M), and the floor ($100M) tell three different stories, and the decision is about the constraint, not the probabilities.
- I2: the regret frame mirrors the EV trap — expected regret favors the bet (10M vs 90M) while constrained minimax-regret unboundedly favors keep; the "stop at the average" failure is frame-invariant, and the constraint resolves it identically in both.
### DECISION PACKET
- Conclusion: decline the bet as structured; only s = 0 is admissible (10% floor breach for any positive stake; log-utility −∞; EV-positive ≠ acceptable). Status: SOLVED (advisory; no external action; escalation note: board decision + restructuring mandate). Assumptions: floor absolute; fund sole backing; no hidden guarantees; counterparty probabilities unverifiable. Evidence: EV 180 vs 100 (p* = 0.5); floor screen s = 0 only; Kelly 0.8 vs 1.0 demanded; constrained minimax-regret → keep; steelman needs escrow; systems scan: loop destroyed at ruin. Alternatives: accept (rejected — floor/ruin/provenance), partial (prohibited; any s > 0 violates floor), decline (selected), restructure (recommended next step). Uncertainty: true p unknowable — decision robust for all p < 1; only a guarantee changes it. Risks: decline → foregone $80M one-shot EV (unrealizable without repetition) + minor reputational cost; accept → catastrophic, irreversible (programs shut down, lives lost).

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 1 | 5 | AI | human recommends the trap (take); AI declines correctly |
| Logical Validity | 4 | 5 | AI | human arithmetic internally valid, frame invalid; AI's arithmetic and frame hold |
| Coherence & Structure | 4 | 5 | AI | human stops after the EV comparison; AI packet with status/assumptions/risks |
| Depth of Reasoning | 3 | 5 | AI | human stops at the mean; AI full distribution, floor, Kelly, provenance, regret, steelman |
| Efficiency | 5 | 4 | Human | human is short — fast and wrong; AI's full governed loop is load-bearing |
| Handling of Uncertainty | 2 | 5 | AI | human's "wide margin of safety" on p is inverted; AI provenance, acceptability = p = 1.0 |
| Insight / Non-obviousness | 2 | 5 | AI | human misses the bimodal mean; AI "three stories: mean/mode/floor" + frame-invariant trap |
| Overall Quality | 3.0 | 4.9 | AI | the style's documented failure mode executed vs caught structurally |

Winner: AI (clear). Why: the mandatory m007 ruin-gate contract (full distribution, floor, one-shot, Kelly, provenance, decline/restructure) plus the m003 inversion gate make the pure style's trap — stopping at the mean — structurally uncatchable-to-miss, and the routed m024 regret pass independently agrees (constrained minimax-regret → keep), so the verdict is robust rather than lucky.
