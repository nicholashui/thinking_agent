# v6 Routed AI Trace — m069-POS-01 (blinded)
## Fluxline support tickets — next-quarter action plan
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,product,science,software | g:diagnose,estimate,predict | c:deadline,high_stakes
- Router top3: m069, m001, m015; confident=yes → SINGLE-ROUTE: m069 first-class pass; m001/m015 = context (m001's "ignores given priors" weakness gate-checked by P10: the recorded counts are ordering authority; m015: situation is complicated/decomposable, not complex). Gate (R3): m007 ruin screen (high_stakes). Flags: tempo mode ON (P2, deadline); closed-scope fast path (P8); structure-first scan (S1, finance).
### WHAT — frame + structure-first scan (S1)
- Frame: rank-and-commit plan; target ≥ 70% of 1,240 tickets within 4–5 engineer-days. Structure: 25 mutually-exclusive categories × root-cause table → a pure sequencing problem (decision-tree shape: fix-set × order).
### WHY — P1 input-provenance audit
- MEASURED (trust): category counts (recorded, mutually exclusive); root-cause coverages are SAMPLED ±5% — error budget on every impact figure. ANCHOR: none (no experience numbers). INTERESTED-PARTY: the director's ≥70% target is a management metric, not evidence — the plan must also stand on cost. Compute from the stated counts only (P10).
### HOW — m069 first-class pass (completion contract)
- Split from data: sort + accumulate: 348 SSO (28.1%) → 600 (48.4%) → 786 (63.4%) → 935 (75.4%) → 1,023 (82.5%). 20/80 claim stated exactly: 5 of 25 categories (20%) → 82.5% of tickets; top 4 → 75.4%. One pass, no re-walk.
- Collapse test — coverage arm: SSO 91% → one token-expiry flag (120 s vs 12 h); reset 88% → one SPF/DKIM misconfig; billing 93% → one proration double-run; 429 96% → one no-backoff exporter. Four of five collapse; data sync (88, six roots) FAILS → excluded from the vital set (own mini-Pareto workstream).
- Collapse test — temporal arm (does the tail feed the top?): categories are mutually exclusive as recorded — a tail-category ticket cannot cause an SSO ticket → NO feed → the split is a legitimate action map (the same test would veto a revenue-cohort split; here it licenses the 20/80 move). Tail = 1,240 − 1,023 = 217 tickets (17.5%) across 20 categories → quarterly monitoring cadence, not engineers.
- Divergence (V1–V3): pass and general route AGREE (same four fixes, same order) → proceed, agreement recorded. Alternatives priced and rejected: even-spray (no critical mass), two-cheapest (43% ceiling), more agents (cost compounds).
### GATES — m007 ruin screen (R3)
- Outcome distribution: 855–920 tickets/mo removed; coverage ±5% → 810–900 bounds. One-shot: no — quarter-scoped, reversible (config/guards re-vertible; 30-day re-measure) → no ruin. Floor: SSO fix alone = 317 tickets (26%) for 45 min — guaranteed floor; do-nothing floor = 18% SLA breaches persist. Decline/restructure: if capacity < 4 days, restructure to SSO + proration guard (≈ 490 tickets ≈ 40%). Provenance: coverage sampled ±5%, counts recorded.
### DO — P8 closed-scope fast path + P2 tempo commit
- Fully specified → stages compressed; commit at DO: SSO token-expiry (45 min, ≈ 317) → proration guard (1 d, ≈ 173) → SPF/DKIM (2 d, ≈ 222) → 429 backoff (1 d, ≈ 143) → ≈ 855–920 tickets/mo (≈ 69–74% of volume) ≈ $7,700–8,300/mo ≈ $92–100k/yr, 4.5 days. P3: failure branch priced — fixes not holding → 30-day re-measure; tail growing → quarterly re-sort (the few are allowed to change).
### REVIEW — insight pass (S2, packet gate)
- I1: the temporal arm's "NO feed" verdict is the split's validity license — on this book the same 20/80 machinery that misleads on revenue cohorts is a true action map because the categories are causally independent.
- I2: the brief's tail figure (≈ 177, 14%) does not reconcile with the stated counts (217, 17.5%) — the plan is invariant to it, but it is exactly the sampled-estimate drift the 30-day re-measure exists to catch.
### DECISION PACKET
- Conclusion: fix SSO token expiry, proration double-run, SPF/DKIM, 429 backoff in that order; ≈ 855–920 tickets/mo (≈ 74% of volume) ≈ $7,700–8,300/mo ≈ $92–100k/yr; data sync = separate multi-root workstream; 20 tail categories monitored quarterly. Status: SOLVED (plan only; no external action).
- Assumptions: categories mutually exclusive as recorded; coverage stable ±5%; capacity materializes. Evidence: 1,023/82.5%; collapse coverages 88–96%; effort × coverage × impact ordering; $9/ticket cost math.
- Alternatives: A collapsed-root sequence (selected); B even-spray; C two-cheapest (43% ceiling); D more agents. Uncertainty: coverage ±5% (810–900); tail count 177 vs 217; SLA side-effects unquantified. Risks: scope creep into tail (monitoring-only boundary); root-cause drift (30-day re-measure + quarterly re-sort); capacity shortfall (restructure to floor).

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 5 | 5 | Tie | identical plan: same four fixes, same order, ≈ 74% / ≈ $100k/yr |
| Logical Validity | 5 | 5 | Tie | same split arithmetic; both exclude data sync on the collapse test |
| Coherence & Structure | 4 | 5 | AI | routed pass + ruin gate + packet vs linear walk |
| Depth of Reasoning | 5 | 5 | Tie | human's first-pass collapse-test framing now matched in-pass; AI adds the temporal no-feed arm |
| Efficiency | 5 | 5 | Tie | both claim the split in one pass; P8 compresses the stages |
| Handling of Uncertainty | 3 | 4.5 | AI | AI prices ±5% coverage, failure branch, 177-vs-217 count flag; human asserts |
| Insight / Non-obviousness | 5 | 5 | Tie | human's "spraying 25 categories" vs AI's no-feed validity license + provenance flag |
| Overall Quality | 4.6 | 4.8 | AI | correctness tied; routed pass closed v5's depth/efficiency/insight gaps |

Winner: AI (narrow). Why: the m069 pass ran the collapse test (coverage + temporal arms) and the exact 20/80 claim as first-class single-pass moves — the depth, efficiency and insight dimensions where the non-routed v5 AI lost are closed, and the temporal arm, ruin gate and count reconciliation are additions the baseline lacks.
