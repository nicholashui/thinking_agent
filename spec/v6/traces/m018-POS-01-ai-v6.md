# v6 Routed AI Trace — m018-POS-01 (blinded)
## Rewrite vs incremental modernization — engineering decision (memo + appendix)
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,organization,product,science,security,software | g:decide,estimate,guarantee,maximize,predict | c:high_stakes
- Router top3: m018, m019, m070; confident=no → DUAL-ROUTE (G1): m018 steelman + m019 adversary as first-class passes, synthesized via divergence resolution (§II.4.4); m070 = synthesis context. Mandatory gates: m003 inversion (R4, guarantee-goal), m007 ruin screen (R3, high_stakes).
### WHAT — frame + structure-first scan (S1)
- Frame: approve rewrite (A, 16 dev-months) vs targeted modernization (B) vs status quo (C), judged against the memo's own goal (300 ms p99) on the appendix evidence. Structure-first: the goal gap is a two-term stack — API p99 410 ms + ETL 1.8 s of the 2.4 s; "parity = all API endpoints migrated" excludes the ETL term; decision tree A/B/C with B sequenced ETL-first.
### WHY — P1 input-provenance audit
- MEASURED (trust): appendix — 340 bug-fix commits/36 mo, ~90% manual-QA-only coverage, API 410 ms, ETL 1.8 s, staffing 6 Python + 1 Go, attempts A/B leadership-cancelled (CEO d21, new VP d30), C completed-unshipped. ANCHOR: "band-aid, tried 3× and failed" is a dismissal, not evidence; "2.4 s vs 300 ms" anchors a stack change. INTERESTED-PARTY: the author is the memo's only Go engineer; his cost model omits re-derivation, ETL, and migration-QA line items.
### HOW — style passes (dual-route, completion contracts)
- Pass S1 (m018 steelman — contract: opposing position rebuilt in its STRONGEST defensible form, proponent would accept, BEFORE critique; steelman-vs-critique separated):
- STEELMAN: (1) re-derivation cost — 340 fixes are accumulated domain truth, ~90% invisible behind manual QA; re-derivation ≈ 6–9 dev-months the 16-month parity omits, and the risk concentrates exactly in the behaviors the rewrite bets it can reimplement; (2) goal-scope — the 300 ms target is dominated by the ETL, which parity excludes: even a perfect rewrite cannot hit the goal; (3) staffing — 16 dev-months of Go from 6 Python + 1 Go is a retraining problem; the parity date is not credible on the memo's own numbers; (4) history — two leadership cancellations and one unshipped spike are not approach failures: the incremental path has never been evaluated.
- CRITIQUE (each rebuilt argument, quantitatively): (1) conceded — cuts both ways, answered by sequencing (harness-first, then refactor); (2) stands — no rewrite line item touches ETL; (3) stands; (4) conceded — we are giving the incremental path its first real attempt. Deepest objection ("the 90%-untested risk also menaces B") answered, not dodged: fix ETL first (isolated, measurable), then regression-test-first refactoring with typed contracts.
- Pass S2 (m019 adversary — contract: quantified exposure, baseline-risk first, unconsulted stakeholders): attack the memo line-by-line — (i) parity underpriced: no line for data-invariant re-derivation (≈6–9 dev-months), ETL, or migration QA; (ii) "talent attraction" has no mechanism (hiring line absent; 1-of-7 Go); (iii) no abort/rollback criteria for a 16 dev-month bet; (iv) unconsulted: the 5 Python engineers whose domain knowledge the rewrite discards, QA whose checklists are the only coverage. Baseline-risk first: do-nothing = 2.4 s p99 at 0 dev-months; rewrite failure branch = 16 dev-months spent, goal intact (ETL excluded), team half-retrained.
- Divergence resolution (V1–V3): steelman and adversary passes AGREE (reject A, choose B, ETL-first); the general route agrees — agreement recorded, no calibration pass needed. m070 synthesis: every SWOT item weighted by appendix evidence, not memo framing.
### GATES — m003 (R4) · m007 (R3)
- m003 inversion ("how to guarantee the modernization fails"): ≥6 categories ranked L×I — (1) refactor before the regression harness → 90%-untested behaviors break silently (fatal); (2) ETL not first → goal missed even on full success (fatal to the stated metric); (3) scope creep into an API rewrite; (4) no typed-contract boundary → data invariants drift; (5) no milestone contract → third cancellation pattern; (6) containerize without load tests → gains unmeasured. Residual (un-mitigable): a few legacy behaviors can never be fully re-derived — accepted, bounded by the harness. Never/always: a rewrite is never the first move when the goal gap is outside its scope; modernization always starts at the measurable bottleneck.
- m007 ruin screen: distribution — A: ~15% goal+parity, 40% parity w/o goal (ETL excluded), 35% overrun, 10% abandoned ≈ 16 dev-months sunk with the goal intact; B: ~70% goal in 6 weeks, 20% partial (fallback: batch partitioning), 10% no gain (damage contained by harness-first); C: 0% goal, 0 cost. Ruin: none existential; one-shot: no — B is incremental and reversible; floor: harness-first bounds the untested-behavior loss; Kelly: B at ~2 dev-months vs A at 16 with a lower goal probability; provenance: every probability traced to appendix facts (ETL share 1.8/2.4, staffing counts, attempt records), none from the memo's framing.
### DO — P3 branch completeness + closed-scope fast path (P8)
- Fully specified by the appendix → stages compressed. Negative branch priced: "modernization under-delivers" = fallback (partition the batch job, second 2-week item); "rewrite approved" = 16 dev-months + 1.8 s ETL intact + 6–9 dev-months of unpriced re-derivation. No external action (recommendation memo); verdict committed at DO: reject A; fund 6-week ETL-first B (≈2 dev-months ≈ 1/8 cost).
### REVIEW — insight pass (S2, packet gate)
- I1: the memo's strongest argument is its own cleanest refutation — it bets 16 dev-months on re-finding what 340 fixes encoded, and it fails its own metric: the goal gap lives in the ETL, which "feature parity" excludes.
- I2: "tried 3 times and failed" is evidence the incremental path was never evaluated, not that it failed — the dismissal is the straw-man; set aside, the history favors giving it its first real attempt.
### DECISION PACKET
- Conclusion: reject the rewrite; adopt the 6-week targeted modernization (parallelize/stream ETL first, containerize, typed contracts, regression harness around the manual-QA behaviors) ≈ 2 dev-months, reaching 300 ms p99 at ~1/8 cost; survivor test passed — the strongest rebuilt case, fully addressed, no longer wins.
- Status: SOLVED (recommendation delivered; alternatives verified). Assumptions: appendix facts = ground truth; latency target = end-to-end p99; staffing roster fixed. Evidence: appendix (340 commits, ~90% manual-QA, 410 ms API, 1.8 s ETL, 6+1 staffing, A/B/C history); memo scope line vs ETL absence; recomputation 0.41 + 1.8 ≈ 2.2–2.4 s.
- Alternatives: A (rejected: goal-failure, staffing, underpriced parity) · B (selected) · C (rejected: fails goal). Uncertainty: ETL gain band 1.6–2.0 s; P(300 ms) ≈ 70–85%; re-derivation residual bounded by harness-first. Risks: untested-behavior regression during refactor (mitigated: harness before refactor); rewrite recurrence if B under-delivers (revisit only on measured failure).

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 5 | 5 | Tie | both hit all 5 rubric points: straw-man set aside, 4 rebuilt args, quantitative address, outcome change, survivor test |
| Logical Validity | 5 | 5 | Tie | human's dialectic complete; AI adds V1–V3 agreement between steelman and adversary passes |
| Coherence & Structure | 4 | 5 | AI | steelman-vs-critique separation is now a contract output; gates + packet make it structural |
| Depth of Reasoning | 5 | 5 | Tie | human costs re-derivation 6–9 dev-months; AI adds adversary's underpriced-parity + 6 inversion categories |
| Efficiency | 4 | 5 | AI | dual passes replace redundant rebuild; no wasted exploration |
| Handling of Uncertainty | 5 | 5 | Tie | human concedes the untested-behavior risk and sequences; AI adds full distribution + provenance |
| Insight / Non-obviousness | 5 | 5 | Tie | both reach the goal-failure twist via the opponent's best case, not the memo's framing |
| Overall Quality | 4.9 | 4.9 | Tie | the v5 gap (AI 3.7: inherited frame, no survivor test) is closed — same decision, same epistemic status |

Winner: Tie (vs v5 human baseline). Why: the routed dual-pass (m018 steelman first-class with the strongest-form + separation contract, m019 adversary) plus m003/m007 gates converted the v5 AI's inherited-frame cost-benefit into the full dialectic — straw-man named and set aside, all four evidence-grounded arguments rebuilt and addressed quantitatively, deepest objection answered by sequencing; the survivor test the non-routed run skipped is now a mandatory pass output.
