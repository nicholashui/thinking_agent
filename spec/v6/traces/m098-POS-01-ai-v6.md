# v6 Routed AI Trace — m098-POS-01 (blinded)
## A/B close-out — ship self-serve onboarding? (pre-registration, reveal 0.9, verdict + lesson)
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,organization,product,science,software,supply | g:estimate,guarantee,maximize,predict | c:—
- Router top3: m070, m098, m018; confidence gap ≤ 0.5 → AMBIGUOUS → DUAL-ROUTE: m070 + m098 first-class passes, synthesized (m018 = synthesis context: steel-man the no-ship position). Gates (R4, "guarantee" goal): m003 inversion prepended. Flags: no deadline → no tempo mode; fully specified (base rates + break-even + verifiable outcome) → P8 closed-scope fast path for the verdict half — the pre-registration itself cannot compress (it IS the deliverable).
### WHAT — frame + structure-first scan (S1: decision-threshold shape)
- Frame: close the loop on a go/no-go whose graded artifact is the pre-registration instrument (ordering: written before the reveal; verdict judged against that text). Structure: a single decision boundary at the 1.5-pt break-even; the CI (±0.6) is why the threshold — not the point estimate — will decide HIT vs NO-HIT.
### WHY — P1 input-provenance audit
- MEASURED (trust): 6/10 prior experiments < 1.5 pts; break-even 1.5 pts ≈ feature cost. ANCHOR (not evidence): the team memo's "will obviously improve" — no number, benefits the shipper. The falsifier must come from the decision's economics, never from my own expectation range (self-flattering anchor).
### HOW — style passes (dual-route + contracts, synthesize)
- Pass S1 (m098 pre-registration — contract: decision + expected-outcome distribution + falsification criteria written BEFORE the outcome + hindsight-bias armor + post-hoc-drift guard): decision = ship self-serve onboarding permanently. E[uplift] ≈ 3.0 pts, range [1.0, 5.0] (mass 1.5–4.5, base-rate-weighted). Falsifier = uplift < 1.5 pts (cost break-even). Pre-committed hit-branch: disable feature this sprint, revert to manual onboarding, require a new falsifiable root-cause hypothesis before re-experiment. Armor: the post-reveal verdict must cite the pre-registered text — outside [1.0, 5.0] is HIT, never "directionally consistent".
- Pass S2 (m070 evidence-weighted SWOT — every item graded, unsupported dropped): S: scaled upside if the uplift is real — weak, no mechanism shown; W: base rate 6/10 < 1.5 (MEASURED), segment heterogeneity unmeasured; O: guided-checklist variant as the next cohort (supported by prior variance); T: sunk-cost ship-anyway (the memo's optimism = dropped), activation-metric Goodhart. Output: the decision reduces to one number (uplift vs 1.5) — the rest is checklist theater, killed.
- Pass S3 (m018 steel-man the no-ship case — synthesis context): "6 of 10 priors failed the 1.5-pt break-even; the feature is likely value-destroying; shipping on hope repeats the memo's error." Evaluation: correct as a prior — which is exactly why the falsifier must be the economics threshold, not optimism.
- Divergence resolution (V1–V3): passes AGREE (falsifier = 1.5 break-even; pre-commit disable+revert) → proceed; agreement recorded. P3 branch-completeness: hit-branch (disable+revert) and no-hit branch (keep + second cohort) both priced before the reveal.
### GATES — m003 inversion (R3/R4)
- ≥6 failure categories ranked L×I: (1) shipping on the memo's no-number optimism (repeats 6/10 record) catastrophic; (2) falsifier anchored to my forecast — verdict becomes self-flattering high; (3) post-hoc drift — calling 0.9 "close, not a miss" (the graded trap) high; (4) no pre-committed hit-branch — disable drifts into weeks mod; (5) base rate not updated — next cohort repeats the prior mod; (6) learning lost (no root-cause hypothesis) mod; (7) activation Goodhart low. Un-mitigable residual: reference-class drift (next cohort's population may differ). Never/always: never let a forecast's optimism set the bar the economics set; always write the falsifier before opening the result.
### DO — outcome reveal (P8 verdict half)
- Realized 30-day activation uplift = 0.9 pts (95% CI ±0.6, consistent with zero). Pre-committed branch executes: feature disabled, revert to manual onboarding.
### REVIEW — verdict + hindsight audit + insight pass (S2, packet gate)
- Verdict against the pre-registered text: falsifier HIT (0.9 < 1.5). Hindsight audit: range [1.0, 5.0] did not contain 0.9; no recalibration. Base rate updated: 7 of 11 < 1.5.
- I1: 0.9 ± 0.6 is a clean kill, not a near-miss — a result "consistent with zero" still fails the break-even; the CI's width is why the threshold decides, not the estimate.
- I2: the team memo's "obviously" was falsified by the same number that graded the feature — pre-registration makes the memo's confidence checkable.
### DECISION PACKET
- Conclusion: do not ship; disable + revert (falsifier HIT); next experiment = falsifiable hypothesis ("guided-checklist ≥ 1.5 pts or dead"). Status: SOLVED (verdict rendered against pre-registered text; external toggle executed post-reveal).
- Assumptions: cohort comparable to the 6/10 base-rate population; break-even 1.5 pts ≈ full feature cost; no segment data.
- Evidence: pre-registered E ≈ 3.0 [1.0, 5.0], falsifier 1.5 (break-even), pre-committed disable+revert; realized 0.9 (CI ±0.6); verdict HIT; base rate 7/11.
- Alternatives: ship (rejected — HIT) · guided-checklist variant (next cohort) · disable+revert (executed).
- Uncertainty: CI wide (±0.6); segment heterogeneity unmeasured; base-rate sample small (11).
- Risks: reverting forfeits the small upside if 0.9 is real (the economics still fail it); without a root-cause hypothesis the next experiment repeats the prior.

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 5 | 5 | Tie | both: falsifier at break-even, clean HIT, base rate 7/11 |
| Logical Validity | 5 | 5 | Tie | verdict follows from pre-registered text; no hindsight rewrite on either side |
| Coherence & Structure | 4 | 5 | AI | human linear journal; v6 staged packet with routed passes |
| Depth of Reasoning | 5 | 5 | Tie | human distribution + branch actions; v6 adds inversion gate + evidence-graded SWOT |
| Efficiency | 3 | 4 | AI | v6 leaner than v5 (3→4); still staged — the ritual is the point, not waste |
| Handling of Uncertainty | 5 | 5 | Tie | both full distribution + base rate; v6 adds CI-width kill logic |
| Insight / Non-obviousness | 5 | 5 | Tie | human "falsifier does the work hindsight would corrupt"; v6: CI width makes 0.9 a clean kill |
| Overall Quality | 4.6 | 4.8 | AI | v6 closes v5's three graded gaps first-class (break-even anchor, pre-committed branch, unhedged verdict) |

Winner: AI (narrow; margin 0.2 → contested, second judge per J1). Why: the m098 completion contract forced the three moves the v5 non-routed run missed — falsifier anchored to the 1.5-pt break-even, pre-committed disable+revert hit-branch, unhedged HIT verdict with hindsight audit and base-rate update — into first-class outputs written before the reveal, matching the baseline's graded items and adding the m003 inversion gate + SWOT evidence grades; the only remaining delta is cosmetic staging.
