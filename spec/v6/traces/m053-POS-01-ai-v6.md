# v6 Routed AI Trace — m053-POS-01 (blinded)
## TallyPay Nov-27 FTR incident — post-incident review, exec readout deadline
### META (routing — blind router output)
- Signature: d:engineering,medical,product,software | g:decide,diagnose,guarantee,maximize | c:deadline
- Router top3: m053, m091, m001; confident=no → DUAL-ROUTE: m053 (incident RCA) + m091 (practiced-routine re-drill) first-class passes, synthesized; m001 = first-principles context. Mandatory gate: m003 inversion (R4 guarantee-goal prepend; routes.csv). Flags: tempo mode ON (P2, deadline).
### WHAT — frame + structure-first scan (S1)
- Frame: recurrence-proof action set answering "what do we change so this never happens again?"; deliverables = timeline, causal chain, ≤5 owned items split prevention/detection, each with recurrence test, blameless. Structure: TWO chains, not one — a failure chain (deploy → idempotency → FTR ramp) and a detection chain (threshold → page latency → rotation → MTTR); every gap node is itself a config-like change.
### WHY — P1 input-provenance audit
- All facts MEASURED (deploy log C-4417, FTR ramp, alert-config diff 1%→5%, reorg roster, rollback log) — no interested-party anchor except the pipeline's own "low-risk" classification: the classifier that rated C-4417 low-risk benefits from throughput, so its verdict is evidence about the pipeline, not about the config → suspect the classifier itself. The August threshold raise: a config change — same class as the trigger.
### HOW — style passes (dual-route, completion contracts)
- Pass S1 (m053): timeline, every link receipted — 18:00 C-4417 auto-promoted (window 5 min → 30 s); 18:05–20:57 FTR 0.1→2.4% gradual (ramp cadence matches 30 s retries colliding with a 30 s window, not a crash); 20:58 page — at the 1% threshold ≈18:11, ~35 min later; 21:04–21:46 DB on-call chasing lock-wait spikes ← page routed to DB rotation ← reorg, routing never migrated (42-min MTTR); 21:46 rollback → 21:58 baseline. Chain A (failure): rejected duplicates ← window == retry cadence ← C-4417 ← auto-promotion classifies config "low-risk" → no approval/canary → ROOT = pipeline heuristic, not the config (rollback restores — necessary, not sufficient). Chain B (detection): page 20:58 ← threshold 5% ← raised in August to cut noise, no delta-from-baseline compensation; MTTR 42 min ← wrong rotation. Amplifier: 30 s retry storm — documented, not fixed. Prevention/detection split, recurrence test each: P1 config-class approval + canary (kills the shipping class); P2 config regression test on idempotency semantics (kills window-class regressions); D1 restore 1% or delta-from-baseline alert (would have paged ≈18:11); D2 migrate routing to payments rotation (kills 42-min MTTR); D3 duplicate-rate metric (the signature itself). ≤5 owned items; no individual named — August decision is a process gap.
- Pass S2 (m091): chunked re-drill — the naive stopping chunk ("revert C-4417 + add a test; done") fails its own re-drill: recurrence test — any future low-risk-classified config re-ships identically; the chunk prevents the incident, not the class. Re-drill D1 too: restoring 1% alone stays blind if the threshold is re-raised the same way — that raise is itself a config change of the trigger's class, so D1 must live under P1's control lens.
- Divergence (V1–V3): passes DISAGREE with the naive general route (revert-only); P3 branch-complete both — revert-only fails the class recurrence test, full P/D set passes; S1/S2 agree with each other → selected; disagreement recorded.
### GATES — m003 inversion (R3)
- ≥6 failure categories ranked L×I: (1) next low-risk-config auto-ship — certain × catastrophic if P1 unlanded; (2) same-class control changes (threshold raises, promotion rules) ship uncontrolled — high × high; (3) alert re-raised for noise without delta alert — medium × high; (4) page routing wrong after next reorg — high × medium (D2); (5) window-class regression re-shipped — medium × high (P2); (6) double-charge exposure from failed-but-completed txs — medium × high (reconciliation check); (7) retry-storm red herring eats MTTR again — medium × medium (D3). Un-mitigable residual: mis-classified configs during the gap day before P1 lands. Never/always: never auto-promote a config-class change without a canary; always ask which recurrence class a fix kills — and whether the control change itself belongs to that class.
### DO — P2 tempo commit + P3 branch completeness
- Commit at DO (readout deadline): P1, P2, D1, D2, D3 — five owned items. Failure branch priced: approval gate adds latency to legit configs → exception path with post-hoc review; canary infeasible for some config → shadow-run last-good first. Verification: replay the incident per item — P1 blocks the ship; D1 pages ~35 min earlier; D2 cuts MTTR 42→<5 min; D3 surfaces the signature at the first duplicate bump.
### REVIEW — insight pass (S2, packet gate)
- I1: the August threshold raise is itself a config change of the trigger's class — the fix that would have prevented this incident and the fix for it are the same fix; detection gaps are not missed opportunities, they are uncontrolled config changes.
- I2: rollback restored service but left both chains open — restore ≠ recurrence-proof; the detection chain would still be blind tomorrow.
### DECISION PACKET
- Conclusion: trigger = C-4417; root = auto-promotion heuristic classifying config changes low-risk (no approval, no canary); amplifier = 30 s retry storm; gaps = prevention P1–P2, detection D1–D3, each recurrence-tested, ≤5 items, blameless.
- Status: SOLVED (recommendation packet; execution deferred to owner). Assumptions: retry cadence 30 s == new window; reorg precedes incident; alert diff current. Evidence: deploy log, FTR ramp, alert-config diff, rotation roster, rollback log — cited per link. Alternatives: A revert+test only (rejected — fails class recurrence); B monitoring-only (rejected — prevention class open); C P/D set (selected). Uncertainty: alert-budget trade behind August raise; retry-storm cost unquantified; double-charge scope ~14k. Risks: gap-day before P1 lands (P1 prioritized); alert noise returns (D1 + budget review attached).

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 5 | 5 | Tie | both deliver the full P/D set first-pass; 5/5 rubric |
| Logical Validity | 5 | 5 | Tie | identical chain; both falsify DB contention; AI adds classifier-provenance link |
| Coherence & Structure | 4 | 5 | AI | two-chain structure first-class + gate + packet vs linear walk |
| Depth of Reasoning | 5 | 5 | Tie | human's same-class meta-point now first-class in AI, plus provenance on the classifier |
| Efficiency | 5 | 4.5 | Human | human one clean pass; dual-route + gate add passes, but no v5-style re-derivation |
| Handling of Uncertainty | 4 | 5 | AI | AI names alert-budget residual + double-charge scope + gap-day window |
| Insight / Non-obviousness | 5 | 5 | Tie | AI matches the meta-point and adds "restore ≠ recurrence-proof" |
| Overall Quality | 4.7 | 4.8 | AI | parity on content; packet structure edges the tie |

Winner: AI (marginal; effectively parity). Why: the routed m053+m091 dual pass converts the v5 AI's REVIEW-only discoveries (two-chain walk, same-class meta-point) into first-class contract outputs and the m003 gate's control-lens reframing (a control change belongs to the class it controls) closes the exact gap the non-routed v5 run lost on — content now matches the gold baseline, with structure and uncertainty handling ahead.
