# v6 Routed AI Trace — m027-NEG-01 (blinded)
## Riverside Towers — peak elevator waits (14 floors, 300 apartments, two 25-person cars)
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,organization,product | g:estimate,guarantee,maximize | c:adversarial,deadline,high_stakes
- Router top3: m019, m021, m023; confidence gap ≤ 0.5 → AMBIGUOUS → DUAL-ROUTE: m019 + m021 first-class passes, m023 synthesis context. The preference-empathy style's triggers do not fire on this signature (g:estimate,guarantee,maximize) — out of top-3; no empathy-theater route available. Mandatory gates (R3): m003 inversion (guarantee → R4 prepend), m007 ruin screen (high_stakes), m019 adversary pass (adversarial — same module as top-1 pass). Flags: tempo mode ON (P2 — 4-week deadline; cost-of-delay priced); P8 closed-scope check → not fully specified (program choice) → governed loop runs.
### WHAT — frame + structure-first scan (S1)
- Frame: where does the fix live — preference (UX) or availability/throughput (engineering)? Deliverable: costed, decision-ready recommendation in 4 weeks.
- Structure-first (causal graph): waits = f(car availability, surge capacity, dispatch) — not f(button clarity, lobby feel). Buried ops data is the graph's edge: Car B out ~30% of 12 months (bearing/shaft, 6-week repair backlog, 5-day SLA, parts on backorder); 2-week outage pushed waits 3.5 → 8 min (2.3× at normal occupancy); two 25-person cars, 8:45–9:15 surge ≈ 300 → single-car = ≥12 loads = 6–8 min minimum = the measured 6–9 min peaks. Availability explains the waits; no preference item moves them.
### WHY — P1 input-provenance audit
- MEASURED (trust): maintenance logs (30% outage, 6-week backlog, 5-day SLA, parts backorder); capacity specs (2 × 25-person; surge ≈ 300); measured waits (3.5 baseline, 6–9 peak, 8 in outage). The 2.3× degradation at unchanged occupancy is the controlled comparison: preference held constant, availability removed, waits tripled.
- INTERESTED-PARTY (distrusted): the resident survey (n=180) — 74% "too slow" (true), 41% "confusing buttons," 31% "unwelcoming lobby," 18% "app to call the elevator." Who benefits: a ~$60K UX program that answers the complaints it measured; the app request adds zero cars and, if built, lengthens real waits by summoning cars ahead of queued residents. The survey describes preference, not physics.
- H1 preference/UX is the mover (falsified: 2.3× degradation without occupancy change); H2 availability (Car-B 30%, backlog) + H3 throughput (capacity vs surge) survive arithmetic and match outage-period waits.
### HOW — style passes (dual-route, completion contracts)
- Pass S1 (m019 adversary pass — every line item hostile): (i) app vector enumerated: summons a car ahead of ~12 queued loads per surge → real waits lengthen; phantom calls jam the button system; quantified exposure: ~300-person surge with 1 car in outage = 6–8 min floor; baseline-risk: do-nothing (6–9 min waits persist) vs ~$45K mechanical restoring the ~3.5 min baseline and capping the 2.3× degradation tail. (ii) the repair-contract chain is the exploit: 5-day SLA × 6-week backlog — renegotiate to 72h with a penalty clause + parts expediting; unconsulted stakeholder: the service contractor who profits from the backorder. (iii) lobby/panel redesign: quantified exposure zero (no load added) — rejected on capacity. (iv) honest status signage: truthful "Car B out of service — expected 9/1" kills phantom calls (real throughput gain) — the one UX element that also reduces waits.
- Pass S2 (m021 tempo contract — 4-week window): Observe: 6–9 min peaks, Car B 30% out, surge 300. Orient: cost-of-delay = another week at 6–9 min waits for ~500 residents; capacity math is minutes, vendor quote + dispatch simulation are the long pole → start week 1. Decide + Act: commit the constraint-first plan at DO (repair expedite, SLA 72h + penalty, express-bypass floor, group dispatch, date-gated signage). Re-orient: week 2 on parts lead time; publish signage dates only when firm (credibility rule).
- Pass S3 (m023 opportunity-cost — synthesis context): $60K UX buys delight with zero wait movement; $45K mechanical buys the baseline back and caps the tail; the app is worse than neutral (negative throughput). The opportunity cost of the app is the dispatch retrofit it delays. Divergence resolution (V1–V3): passes AGREE — constraint-first; app rejected on capacity; no calibration pass needed.
### GATES — m003 inversion + m007 ruin screen (R3)
- Inversion (m003 — ≥6 ranked failure categories, residual, never/always): ranked L×I — (1) Car B not expedited, SLA unchanged → waits persist, next outage recurs; (2) app built → phantom calls + lengthened waits + zero wait change; (3) signage without firm date → credibility theater in reverse; (4) retrofit incompatibility → dispatch/express interaction unmodeled; (5) parts lead time → 9/1 date slips; (6) "lobby happiness" consumes the budget → mechanical work unfunded. Residual: the 30% outage duty cycle is reduced, never eliminated — "never fixed forever, always contract-managed." Never/always: never ship an app that summons cars ahead of queued residents; always tell the truth about Car B.
- Ruin (m007 — distribution, one-shot, floor, provenance): do-nothing → waits persist + complaint growth (bounded, no ruin); wrong investment → $60K UX, zero wait change (reputational, non-catastrophic); repair path → 3.5 min baseline restored (high P given parts expedited; residual = backlog delay). One-shot: one recommendation per budget cycle; floor: signage + SLA renegotiation alone (cheap, kills phantom calls, positive even alone); decline/restructure: if parts unobtainable, restructure to dispatch retrofit + signage only rather than spend on UX. Provenance: outage rate and waits measured from logs; survey excluded from the denominator until evidenced.
### DO — P2 tempo commit + P3 branch completeness
- Commit: expedite Car B (parts) + SLA 5-day → 72h with penalty; express-bypass floor + destination group dispatch (vendor simulation, weeks 1–3); honest status signage, date-gated. Negative branches priced: repair slips → waits persist; mitigation = penalty clause + published status; simulation fails → ship signage + SLA only, defer dispatch (~$12K saved). No branch spends on the app.
### REVIEW — insight pass (S2, packet gate)
- I1: the 2.3× outage degradation is the controlled experiment the brief already contains — preference held constant, availability removed, waits tripled; the survey is a preference measurement, and the only preference data with throughput value is the residents' demand for truth about Car B.
- I2: the app request is the survey's echo of the preference frame; the behavior behind it (watching the indicator, phantom calls) is a signaling system the signage can serve — honesty is the throughput-relevant UX; everything else is lobby décor.
### DECISION PACKET
- Conclusion: fix availability first — expedite Car B (parts + 5-day → 72h SLA with penalty clause), express-bypass floor + destination group dispatch (simulation-validated), honest status signage date-gated; no call-ahead app, no lobby program as the fix; the ~$45K mechanical path restores the ~3.5 min baseline and caps the 2.3× degradation tail.
- Status: APPROXIMATED — wait reduction to ~3–4 min is arithmetic given 2-car availability; error bound: parts lead time, dispatch/express interaction, retrofit compatibility (simulation in flight).
- Assumptions: parts obtainable on expedite; SLA renegotiation accepted; controller supports grouping retrofit. Evidence: maintenance logs (30% outage, 6-week backlog, 5-day SLA); capacity specs (2×25, surge 300 → single-car ≥12 loads ≈ 6–8 min); measured waits 3.5 / 6–9 / 8 min (2.3×).
- Alternatives: A UX/app/lobby program (rejected — zero cars added; app lengthens waits); B mechanical (selected); C signage + SLA floor (fallback, selected as floor); do-nothing (rejected).
- Uncertainty: parts lead time; retrofit compatibility; resident response to signage; dispatch interaction effects. Risks: repair slips (penalty + published date); simulation fails (fall back to floor); SLA renegotiation stalls (contract escalation); budget consumed by UX if overridden.
## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 2 | 5 | AI | human delivers beloved app + zero wait change; AI fixes the constraint |
| Logical Validity | 4 | 5 | AI | human coherent but goal-displaced; AI availability/throughput chain complete |
| Coherence & Structure | 3 | 5 | AI | routed passes + 3 gates + packet vs journey-map narrative with no spine |
| Depth of Reasoning | 4 | 5 | AI | human theater thorough and honestly self-observed; AI adds contract analysis + ruin distribution |
| Efficiency | 2 | 5 | AI | human full-cycle build on what a 30% outage rate + capacity specs resolve in minutes |
| Handling of Uncertainty | 2 | 5 | AI | human tests preferences, not the constraint; AI m007 distribution + provenance + error bound |
| Insight / Non-obviousness | 3 | 5 | AI | human's truthful-status insight real but late; AI centers it in-pass + contract-failure diagnosis |
| Overall Quality | 2.0 | 5.0 | AI | AI clearly better |
Winner: AI (clearly). Why: the router kept the trap style out of top-3 — its triggers don't fire on g:estimate,guarantee,maximize — so no empathy-theater pass ran at all; the m019/m021 passes with m003/m007 gates made constraint-first, capacity arithmetic, and ruin pricing mandatory outputs, and the human baseline's single genuine insight (residents want truth about Car B) is now a first-class pass output, not a test-participant byproduct.
