# v6 Routed AI Trace — m008-NEG-01 (blinded)
## Aurora WAU forecast — category-new, no reference class, resolves week 8
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,product,software,strategy,supply | g:decide,estimate,maximize,predict | c:adversarial,unmeasured
- Router top3: m044, m070, m011; confidence gap <= 0.5 → AMBIGUOUS → DUAL-ROUTE: m044 + m070 first-class passes, synthesized (m011 systems scan = synthesis context). m008 not routed (estimate-only trigger match; decide-heavy signature). Gates (R3): m006 provenance audit (unmeasured), m019 adversary pass (adversarial). P2 tempo OFF (no deadline); P8 NOT available (evidence gap unfillable now; resolves week 8).
### WHAT — frame + structure-first scan (S1)
- Structure: growth curve with an UNMEASURED retention leak driving decay; decision tree = ramp size × outcome. The demanded deliverable (committed number ± tight CI) is not warranted by n = 2; deliverable = honest wide probability + revision rules + decision lever.
### WHY — P1 input-provenance audit
- MEASURED (trust): 2 beta points (1,800 → 2,200, +22%/wk). ANCHOR-adjacent: 1-parameter viral fit from 2 points — 0 residual df; any CI width is asserted, not measured. INTERESTED PARTY: CEO X-post (self-serving, zero metric content); bug-fix claim (operational, unverified). Cross-case: 3 category-adjacent comparables decayed/failed — anecdotal, uncounted.
### HOW — style passes (dual-route, synthesize)
- Pass m044 (stakeholder): board needs a governance number — the decision lever (staged ramp + kill criteria) serves it; product's "commit to a number" converts precision into liability, not information; the demand constrains the DECISION, not the forecast.
- Pass m070 (evidence-weighted SWOT): internal — growth +22%/wk (n = 2, weight low); missing — retention, channel mix, activation (no-evidence dominates); external — 3 comparables decayed (anecdotal). Evidence-weighted conclusion: lean to the category-new base rate (P ≈ 0.10–0.15), NOT to the model tail.
- Pass m011 (systems scan, synthesis): stock = WAU base; flows = install velocity (in), retention (leak), activation share (funnel); loop = viral coefficient — UNIDENTIFIED (a 2-point curve fit is not a loop measurement). Falsifiable observable: week-2 post-launch retention ≥ 30% confirms the loop; < 15% falsifies it (P ≤ 0.05). Local-data-first: the 2 beta points are the only local data; the extrapolation beyond them is the model's, not the data's.
- Synthesis (V1–V3): all passes AGREE → wide range, low-moderate P, hold flat on non-events, stage the ramp; agreement recorded in packet.
### GATES (R3) — m006 provenance audit (unmeasured)
- Likelihood scenarios: S1 viral fit holds → P ≈ 0.35, CI [8.2k, 11.5k] (the model's claim); S2 category-new regression to base rate → P ≈ 0.10–0.15; S3 retention leak (week-2 < 20%) → P ≈ 0.05. Posterior range [0.05, 0.35]; best ≈ 0.15.
- Threshold flip: NO in-hand evidence can lift P past 0.5 — flip requires named data (week-2 retention ≥ 30% AND install velocity ≥ 5k/wk → P ≥ 0.5). Decision flip at P ≈ 0.3–0.4 → staged ramp decouples the decision from forecast precision.
### GATES (R3) — m019 adversary pass (adversarial)
- Vectors quantified: (1) theater precision (0.33/[8.2k–11.5k]) → full ramp on P ≈ 0.33 where honest P ≈ 0.15 — exposure ≈ 0.75× ramp cost + reversal; (2) model-asserted 90% CI with 0 residual df — decisions keyed to a coverage claim with no evidence; (3) CEO X-post as evidence → governance on narrative; (4) "commit to a number" anchoring → overcommit. Unconsulted stakeholders: analytics/product (retention data), marketing (channel mix). Baseline-risk: no forecast → coin-flip on an irreversible ramp; theater → overcommit; honest wide + staged ramp → cheapest expected loss.
### DO — P3 branch-completeness
- Branches priced: full ramp + YES (capacity ready; weight 0.15); full ramp + NO (0.85) → full ramp cost wasted + reversal; staged 25% + YES (catch-up hire at week 3, priced); staged + NO (right-sized). Staged wins both branches → commit: P = 0.15, 90% range [1k–30k]; probability HELD FLAT across the 3 non-events (no new metric information — logged); week-2 retention dashboard = cheap decisive experiment (m011).
### REVIEW — insight pass (S2, packet gate)
- I1: the board's demanded "precision" is a liability transfer — a theater CI converts model-asserted certainty into a staffing commitment; the honest wide range is the cheaper risk.
- I2: with 0 residual df, ANY interval width is asserted — the only defensible width is set by the category-new base rate, not by the fit; the fit prices nothing.
### DECISION PACKET
- Conclusion: P(Aurora ≥ 10k by week 8) ≈ 0.15; 90% range [1k–30k]; flat across non-events; ramp staged at 25% with week-2 kill criteria; retention dashboard shipped week 1.
- Status: NEEDS_EVIDENCE (missing: retention, channel velocity, activation share; L1 degrade; resolves week 8). Assumptions: category-new products regress; beta weeks ≠ launch acquisition.
- Evidence: 2 beta points; 0 residual df; 3 non-events with zero metric content; no reference class (verified).
- Alternatives: theater 0.33/[8.2k, 11.5k] (rejected: asserted width, missed outcome); no forecast (rejected: decision needed); honest wide 0.15 (selected).
- Uncertainty: very high; range deliberately wide (±10 pp); scenario band [0.05, 0.35]. Risks: under-hire if H1 right (catch-up clause); overhire if wrong (25% staging caps); ramp reversal cost.
## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 2 | 5 | AI | human's narrow CI missed 2,900; AI's wide range contains it + decision lever |
| Logical Validity | 3 | 5 | AI | human built on a 0-df fit; AI rejects asserted precision, prices branches |
| Coherence & Structure | 3 | 5 | AI | human logs theater updates; AI holds flat, gates + packet |
| Depth of Reasoning | 3 | 5 | AI | AI adds scenario band, flip points, unconsulted stakeholders, decisive experiment |
| Efficiency | 4 | 3.5 | Human | routed machinery costs steps (the style's known 'slow' trait); human answers fast |
| Handling of Uncertainty | 2 | 5 | AI | human's 90% CI missed; AI scenario range [0.05, 0.35], wide outcome range |
| Insight / Non-obviousness | 3 | 5 | AI | liability-transfer reframe; 0-df width assertion; base-rate-anchored width |
| Overall Quality | 2.9 | 4.8 | AI | pure style fails its blind spot; routed run avoids all three designed failure modes |

Winner: AI (clearly). Why: the router sent the case AWAY from the calibration trap style (m008 not in top-3 on a decide-heavy signature) to multi-perspective/SWOT passes with m011 systems synthesis, while the mandatory m006/m019 gates made the likelihood-scenario band, the no-update rule, and quantified exposure first-class outputs — where the non-routed v5 AI reached them as late REVIEW afterthoughts and the human baseline converted the discipline into precision theater.
