# v6 Routed AI Trace — m081-POS-01 (blinded)
## Bold Growth memo — "+45%, replicate everywhere" (quarterly revenue table, recommendation only)
### META (routing — blind router output)
- Signature: d:finance,medical,product,science,software,strategy | g:decide,diagnose,maximize | c:(empty)
- Router top3: m081, m084, m028; top-1/top-2 gap <= 0.5 → AMBIGUOUS → DUAL-ROUTE: m081 + m084 first-class passes, synthesized (m028 = synthesis context). No R3 context triggers (c: empty). Flags: P8 closed-scope candidate (table fully specifies the verdict); no tempo mode (no deadline).
### WHAT — frame + structure-first scan (S1)
- Claim: Bold Growth caused +45% (Q1 $100M → Q4 $145M), replicate everywhere. Structure: 2-region × 8-quarter table (adopter vs non-adopter), contribution table, analyst note (industry relaunch + price change in Q1). Deliverable: verdict + recommendation.
### WHY — P1 input-provenance audit
- MEASURED/given (trust): all table figures; analyst note is data, not story. INTERESTED-PARTY: the memo's author — the team that launched Bold Growth owns the "turnaround we needed" line. ANCHOR: none — every check recomputes from the table.
### HOW — style passes (dual-route, synthesize)
- Pass S1 (m081 story-selection audit — completion contract):
  - Lateral — what the narrative leaves out: Region B never adopted, Q1 97 → Q4 140 = +44.3% → differential +45.0 − 44.3 ≈ +0.7 pp, inside quarterly noise ±4 pp (z ≈ 0.2): no incremental effect.
  - Temporal: memo chart starts at the launch quarter (a selection point). Pre-launch slope ≈ +7%/qtr both (A 60→74, B 58→72); boundary quarter A 74→100 = +35%, B 72→97 = +34.7% — the acceleration is the shared market event, in lockstep.
  - Compositional: Customer X = 31/45 = 69% of A's Q1→Q4 increase; ≈30 of B's 43 — the delta is one account's spend.
  - Data-discipline (weakness gate): the growth is real — market expansion + one account; "no incremental effect" ≠ "no growth"; decompose, don't dismiss.
- Pass S2 (m084 org frame): replication is an adoption/attribution problem — regions carry the initiative's cost while the memo's author takes credit; the pilot is the institution that yields credible evidence instead of anecdote-driven scaling.
- Divergence resolution (V1–V3): passes AGREE with the general route (no incremental lift; reject replication) → agreement recorded, no calibration pass needed.
### GATES — mandatory protective checks
- No R3 context triggers (c: empty). R2 pairing: m084 (weakness: abstract) gate-checked — org frame informs the recommendation, never the numbers. Completion contracts met by both passes.
### DO — P8 closed-scope commit
- Recommendation memo: reject "replicate everywhere"; no evidence of incremental lift (differential +0.7 ± 4 pp; shared boundary acceleration; one-account concentration). P3 branch pricing: replicate-everywhere scales a null effect (initiative cost × N regions); do-nothing = 0; randomized pilot across 2–4 regions with holdouts — small cost, and it is the test, not a delay.
### REVIEW — insight pass (S2, packet gate)
- I1: the headline is true yet vacuous — "+45%" is a real number that selects against the comparison that kills it (B +44.3% without the initiative); the story is a selection, not a lie.
- I2: the memo's chart starts exactly where the contradiction begins — both regions jump ≈ +35% in the launch quarter; the arc's first data point is the market event.
### DECISION PACKET
- Conclusion: no evidence of incremental lift; replication rejected; randomized pilot with holdouts proposed (expected differential under null: 0 ± 4 pp).
- Status: SOLVED (verdict recomputed and cross-checked; recommendation-only). Assumptions: figures truthful; regions comparable apart from adoption; no unrecorded confounds.
- Evidence: B +44.3% → differential +0.7 pp (z ≈ 0.2); boundary +35% both regions; pre-launch ≈ +7%/qtr; Customer X = 69% of A's delta.
- Alternatives: replicate everywhere (rejected — scales a null); lateral-only (rejected — misses the market event); audit + pilot (selected); m084 add: per-region adoption incentives and credit attribution.
- Uncertainty: differential 0.7 ± 4 pp; Customer X exogenous; regional team effects uncontrolled (observational). Risks: scaling a null effect; underpowered pilot (region n small); pilot cost/delay.

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 5 | 5 | Tie | identical verdict: reject replication, propose RCT |
| Logical Validity | 5 | 5 | Tie | both recompute differential, boundary jump, composition; no hallucination |
| Coherence & Structure | 4 | 5 | AI | routed dual-pass + gates + packet vs human's linear single lens |
| Depth of Reasoning | 5 | 5 | Tie | AI contract demands all three sweeps up front + provenance + org frame; human's sweep equally complete |
| Efficiency | 5 | 4.5 | Human | human one disciplined pass; dual-route + packet slightly heavier |
| Handling of Uncertainty | 5 | 5 | Tie | both frame differential as 0 ± 4 pp; AI prices pilot branches |
| Insight / Non-obviousness | 5 | 5 | Tie | human: "arc begins where contradiction begins"; AI: true-but-vacuous headline, one-account concentration |
| Overall Quality | 4.9 | 5.0 | AI (narrow) | substance tied; routed audit makes the v5 opportunistic-discovery gap structural, plus P1/m084 additions |

Winner: AI (narrow). Why: the routed m081 story-selection audit made the three counter-data sweeps a demanded contract (v5's temporal check surfaced only at REVIEW), and P1 provenance + the m084 org frame added the interested-party and attribution views the pure lens lacked — verdict unchanged, process closed the gap.
