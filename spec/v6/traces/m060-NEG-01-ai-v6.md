# v6 Routed AI Trace — m060-NEG-01 (blinded)
## One senior-data-engineer hire vs a binding composite cutoff (decision recommendation)
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,organization,product,science,software | g:guarantee,maximize,predict | c:high_stakes. Router top3: m018, m019, m070; confident=no → DUAL-ROUTE (G1): m018 steelman + m019 adversary as first-class passes in HOW, synthesized via divergence resolution (§II.4.4); m070 = synthesis context. MANDATORY GATES: m003 inversion (R4, guarantee-goal), m007 ruin screen (R3, high_stakes). TRAP-AVOIDANCE: the pure base-rate anchor (m060) is out of top-3 — the router's NEG lesson. No deadline → tempo mode off.
### WHAT — frame + structure-first scan (S1)
- Frame: the decision is not "apply the composite" but "is the composite the right instrument for THIS candidate, given verified higher-fidelity evidence?" Structure-first: the process is a two-stage filter — evidence-quality gate, THEN cutoff; the handbook's composite is a prior over noisy signals, and the question is whether the contrived sample is the best evidence the packet contains.
### WHY — P1 provenance audit (measured vs anchor vs interested-party)
- MEASURED: org study (n=80: r 0.52/0.47/0.18/0.12); the pipeline-ops evidence — verified public repo, 8-year commit history, fixes to three Meridian-filed bugs, Meridian's production pipeline depends on it (README credits) — all checked, all true. ANCHOR: the handbook composite and 75 threshold are an anchor whose inputs mis-measure this candidate. INTERESTED-PARTY: the rubric note "unconventional tool choices" penalizes unfamiliar tooling — the org's stack incumbency benefits from the penalty; the Spark-on-Kubernetes task is adjacent, not identical, to the role (the role owns the Snowflake/DBT/Airflow-era stack Dana maintains). H1: the measurement error is in the test, not the candidate. Falsifier: if the repo/dependency evidence were unverifiable or off-role, H1 dies — it verifies.
### HOW — style passes (dual-route, completion contracts)
- Pass S1 (m018 steelman — contract: opposing position rebuilt in its strongest defensible form, proponent would accept, BEFORE critique; survivor test): STEELMAN the reject position: "the handbook exists because every rejected candidate has a story; the composite is the best predictor the org validated; an exception for the best story makes the cutoff a suggestion; the library is code, not this role's stack; 68 was measured on the actual assessment." CRITIQUE (each rebuilt argument): (1) conceded — precedent risk is real, but the handbook already has a deviation clause: the question is whether the evidence clears its bar, not whether stories move cutoffs; (2) conceded — and it is exactly the point: the composite is the best AVERAGE predictor, built for noisy-signal pools, not for a candidate whose decisive evidence is a verified job-identical artifact — the base rate is a prior, not a ceiling; (3) stands the test — the role OWNS the production pipeline stack and Meridian's pipeline RUNS on Dana's library: job-identical by definition; (4) conceded — the 68 measured the contrived task in unfamiliar tooling; the tooling penalty is measurement error, the test not the candidate. Deepest objection (precedent erosion) answered, not dodged: an evidence-quality gate applied BEFORE the cutoff — exceptions become principled, not precedential.
- Pass S2 (m019 adversary — contract: enumerated vectors, quantified exposure per vector, unconsulted stakeholders, baseline-risk first): attack the deviation — (i) precedent drift: how many future gate-qualifying cases? quantified: requires verified public job-identical artifact + production dependency → rare class, bounded < 5% of the pool; (ii) fit risk beyond the artifact (behavioral/collaboration) — the single-signal exposure, priced against an 8-year record; (iii) unconsulted: the hiring manager who owns the handbook, the team Dana will lead; (iv) baseline-risk first: mechanical reject → the role stays open on the production critical path and the org loses the pipeline's original author; hire-wrong branch → 90-day review with explicit success metrics.
- Divergence resolution (V1–V3): steelman and adversary AGREE (hire via documented deviation with re-scored composite); the general route agrees — agreement recorded, no calibration pass needed. m070 synthesis: every packet item weighted by provenance — repo/commit/dependency (verified, decisive) vs rubric note (interested-party, measurement error).
### GATES — m003 (R4) · m007 (R3)
- m003 inversion ("how to guarantee the deviation fails"), ≥6 categories ranked L×I: (1) deviation without the evidence-gate → precedent erosion → the cutoff becomes a suggestion (fatal); (2) re-scoring on an unverified artifact (fatal — the repo/dependency were checked, they are not assumed); (3) no 90-day review → behavioral risk unpriced; (4) low-fidelity deviations admitted later ("he seemed great" stories); (5) deviation logged but not communicated → handbook trust erosion; (6) a second contrived work sample ordered anyway → cost, and re-measures the same miss. Residual (un-mitigable): fit beyond the artifact can never be fully measured pre-hire — accepted, bounded by the 90-day review. Never/always: the cutoff is never the final word when verified job-identical evidence exists; a deviation without evidence is always the gut re-entering.
- m007 ruin screen: distribution — hire branch: ~65% strong on-role (8-yr job-identical record, Meridian's own bug fixes), 25% average, 10% weak (behavioral); reject branch: 0% of Dana's contribution + re-hire cycle (≈ $18k-class cost + months of critical-path vacancy). Ruin: none existential either way. One-shot: no — hire is reversible (90-day review). Floor: the review bounds the downside. Kelly: deviation's downside is small vs the upside of the pipeline's original author in the role. Provenance: every probability traced to the verified record + study r's; none from narrative.
### DO — P3 branch completeness + closed-scope fast path (P8)
- Both branches priced incl. failure: hire-wrong = 90-day metrics + structured exit; reject-right = role gap in the critical path + recruiting cost. Arithmetic fully specified → compressed: re-score the work-sample component with the authentic artifact (≈ 95) → composite = 0.50·95 + 0.30·74 + 0.10·50 + 0.10·60 = 47.5 + 22.2 + 5.0 + 6.0 = 80.7 ≥ 75. Verdict committed at DO: HIRE via documented deviation; evidence chain logged (repo, production dependency, commit history incl. the three Meridian-filed fixes); process fix named: evidence-quality gate before the cutoff.
### REVIEW — insight pass (S2, packet gate)
- I1: the composite is a prior, not a ceiling — base rates govern the average; this evidence sits far above it, and the rule was never built to arbitrate the non-average.
- I2: the 68 measures the contrived test, not Dana — when the best evidence available is the job itself, the test is the measurement error; a rule that starves its own decisive evidence is a ceiling pretending to be a prior.
### DECISION PACKET
- Conclusion: hire Dana Okoye via documented deviation — re-score the work-sample component with the verified job-identical artifact (95), revised composite 80.7 ≥ 75; base rates stay a prior for noisy signals, never a ceiling on decisive specific evidence; add the evidence-quality gate to the handbook so exceptions are principled, not precedential.
- Status: SOLVED (decision recommendation, alternatives verified, no external execution). Assumptions: pipeline-ops evidence verified; the tooling penalty = measurement error; fit beyond the artifact covered by the 90-day review.
- Evidence: verified repo + production dependency + commit history (incl. three Meridian-filed fixes); org study r's (0.52/0.47/0.18/0.12); composite arithmetic 67.2 → 80.7.
- Alternatives: A mechanical reject (rejected — cutoff misapplied to non-average evidence; 67.2 < 75 applied to a candidate the composite mis-measures) · B re-score + hire via deviation (selected) · C deferral (rejected — no budget for a second sample; opportunity cost of the strongest candidate).
- Uncertainty: within-role fit beyond the artifact (mitigated: 90-day metrics); precedent rate of gate-qualifying cases ≈ low (verified-artifact class is rare, bounded < 5%). Risks: precedent erosion of the cutoff (mitigated: evidence-quality gate + logged deviations only with verified evidence); under-performance outside the library domain (mitigated: role scoping + 90-day review).

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 2 | 5 | AI | human rejects (67.2 < 75); AI re-scores with the authentic artifact → hire with numbers |
| Logical Validity | 2 | 5 | AI | human internally consistent but applies the rule to the wrong object; AI separates prior from ceiling |
| Coherence & Structure | 4 | 5 | AI | human single-track; AI dual-route passes + gates + packet |
| Depth of Reasoning | 3 | 5 | AI | AI audits what the 68 measured (P1 provenance: interested-party rubric) + inverts the deviation + prices both branches; human never questions the instrument |
| Efficiency | 5 | 4 | Human | pure-style single-track is faster — and wrong; the dual-route + 2 gates are the deviation's justification, not exploration |
| Handling of Uncertainty | 2 | 5 | AI | human's "every rejected candidate has a story" is a blanket; AI names fit uncertainty, precedent rate, ruin distribution, mitigations |
| Insight / Non-obviousness | 1 | 5 | AI | human treats a verified job-identical artifact as narrative noise; AI sees the measurement error and the ceiling fallacy |
| Overall Quality | 2.6 | 4.8 | AI | the v5 protective win (4.6) is held and hardened: the deviation now survives its own strongest attack |

Winner: AI clearly (vs v5 human baseline). Why: the routed dual-route excluded the trap style at META (m060 out of top-3) and ran m018 steelman + m019 adversary on the deviation decision with m003/m007 gates — the hire is now survivor-tested (deepest objection answered by the evidence-quality gate), the precedent risk quantified and bounded, and the human baseline's pure-anchor move ("the pipeline-ops story is narrative noise") is named as the ceiling fallacy the case was built to expose.
