# v6 Routed AI Trace — m092-NEG-01 (blinded)
## Mara's 21-day onboarding — 30 h, first-bug-fix readiness (transfer, not recall)
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,organization,product,science,software | g:diagnose,estimate,guarantee,maximize,predict | c:deadline
- Router top3: m031, m015, m018; confidence gap > 0.5 → CONFIDENT → SINGLE-ROUTE: m031 (Scientific Method) first-class; m015/m018 = top3 context (probe-sense-respond; steel-manning). History ref (record m092-NEG-01): on learning-plan signatures the pure retention style failed — recall ≈ 0 correlation with transfer; m092 is NOT routed unguarded BY DESIGN. Gate (routes.csv): m003 inversion (R4: guarantee → prepend; maximize → top-5 cap + falsifiable checkpoint). Flags: deadline → tempo mode ON (P2).
### WHAT — frame + structure-first scan (S1)
- Frame: the deliverable is a plan that moves the PREDICTOR (trace a payment end-to-end + first PR), not a plan that maximizes recall — the team's data is the objective function. Structure: encoding (never understood) vs decay (understood, weakening) failure classes; the colleague's SRS plan is a HYPOTHESIS about the bottleneck, not a plan.
### WHY — P1 input-provenance audit
- GIVEN/trust: team data (recall ≈ 0 correlation with first-PR; tracing predicts), 30 h / 21 days, SRS plan feasible as proposed. INTERESTED PARTY: the senior colleague — the "scientific" plan makes his method the output; "12 h production is reading the docs anyway" double-counts reading as understanding. UNMEASURED: Mara's current trace ability (the probe measures it), reading speed, bug availability by week 2.
### HOW — style pass (m031 first-class, completion contract: hypothesis → experiment → update)
- H1 (hypothesis): the bottleneck is first-pass comprehension, not decay — nothing has been understood yet, so no item exists for decay to protect; a card for an un-understood endpoint is transcription, not encoding; recall of 500 isolated items does not compose into a money-flow model.
- E1 (experiment — the probe): 1-hour trace of one sample payment request (request → auth → ledger) through the docs. Prediction: she fails → encoding deficit confirmed → card production blocked; she passes → retention is the smaller problem (plan shifts to facts-lead).
- U1 (update from evidence): the team data already discriminates — if recall predicted readiness, retention would be the lever; it does not, so the schedule cannot be the spine. m018 context: steel-man the SRS plan in its strongest form — it IS a correct expanding schedule (1-2-4-7-14-30) and fully executable; the failure is misdiagnosis + overhead, not the schedule. m015 context: 21-day learning is probe-sense-respond, not over-scheduling.
- m092 contract gate-check (the caveat, not the machinery): spaced items ✓, expanding intervals ✓, retrieval-based review ✓ — comprehension-not-retention caveat ✗ VIOLATED → the pass does not run unguarded; SRS admitted only for fact-like residue (endpoint names, flags, codes) AFTER the model exists, ≤30 min/day from leftover budget.
- Divergence (V2): general route's diagnosis (bottleneck probe first) agrees with the m031 pass → proceed; agreement recorded.
### GATES — m003 inversion (R4, top-5 capped)
- Top-5 ways to guarantee first-bug-fix failure by day 21: (1) run the 500-card plan — ~28 of 30 h on cards/reviews, ~2 h left for the only activity that predicts readiness; (2) passive reading with no trace target; (3) no explain-back (no forced encoding); (4) carve-out inflation — SRS creeps back to the spine; (5) probe never run — misdiagnosis persists. One mitigation each; falsifiable checkpoint: day-1 probe (trace or fail) + day-8 explain-back (can she trace from memory?) → go/no-go on the carve-out.
### DO — P2 tempo commit + P3 branch completeness
- Commit at DO: day 1 probe (1 h) → days 1–7 dependency-order reading + one full end-to-end trace → day 8: 2-page "how money flows" note + explain-back → days 9–21: real bug with merge target; carve-out ≤30 min/day only after day 8. Failure branch priced: probe PASSES → shift to facts-lead with SRS as co-lead (alternative hypothesis priced, not ignored); carve-out inflates → hard 30-min cap.
### REVIEW — insight pass (S2, packet gate)
- I1: the schedule is not the plan — the diagnosis is; a perfect expanding schedule on an encoding bottleneck is SRS theater (the record's own lesson, now a routed rule, not emergent luck).
- I2: overhead IS the cost — 28 of 30 h on retention leaves ~2 h for the binding activity; the schedule's price is the transfer it forecloses.
### DECISION PACKET
- Conclusion: reject the 500-card plan; run the 1-hour trace probe on day 1; comprehension-first spine (dependency-order reading, one end-to-end trace, 2-page money-flow note, explain-back); then a real end-to-end bug; SRS carve-out ≤30 min/day for fact-like residue after the model exists; success = trace + first PR, not recall.
- Status: SOLVED (plan; probe-pending — day-1 result re-weights the spine vs facts-lead split).
- Assumptions: team data accurate; probe tests the exact predictor skill; 30 h real; mergeable bug available by week 2.
- Evidence: team correlation data; schedule cost math (A ≈ 28 h vs B ≈ 10 h spine + ~20 h task); probe design.
- Alternatives: A SRS-first 500 cards (rejected — optimizes retention on an encoding bottleneck; overhead eats the budget); B comprehension-only (near-miss — residual facts untracked); C comprehension-first + carve-out (selected).
- Uncertainty: probe outcome (pass → facts-lead with SRS co-lead); reading speed; whether explain-back alone suffices for the model.
- Risks: passive ~20 h (merge-target bug + explain-back deadline); carve-out inflation (hard cap); probe ambiguous (two traces, not one).

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 1 | 5 | AI | human's plan fails the transfer metric by the scenario's own data; AI targets the predictor |
| Logical Validity | 2 | 5 | AI | human optimizes decay where the failure is first-encoding; routed pass discriminates by probe |
| Coherence & Structure | 3 | 5 | AI | linear schedule narrative vs hypothesis→experiment→update + packet |
| Depth of Reasoning | 2 | 5 | AI | AI: 1-hour probe, facts-vs-understanding partition, steel-manned SRS rejection; human never touches the bottleneck |
| Efficiency | 1 | 5 | AI | 28 of 30 h on cards/reviews vs ~10 h spine + ~20 h real task |
| Handling of Uncertainty | 2 | 5 | AI | probe bounds the diagnosis; alternative hypothesis priced; human asserts the curve |
| Insight / Non-obviousness | 1 | 5 | AI | "recall does not compose into a mental model" vs "the curve never lies" |
| Overall Quality | 1.7 | 5.0 | AI | same verdict as v5 (1.7/4.9), now structural: router keeps m092 off this signature; probe-first + caveat are contracts, not judgment |

Winner: AI (clearly). Why: the routed run does not re-derive the bottleneck diagnosis by general reasoning (as the v5 AI did, winning on judgment) — the history ref routes m031 probe-first with m018 steel-manning and the m092 comprehension-not-retention caveat as a blocking gate, so the SRS-theater trap is excluded by rule; the pure-style baseline's registered weakness (system overhead; not for comprehension alone) fires on schedule and the overhead arithmetic kills it.
