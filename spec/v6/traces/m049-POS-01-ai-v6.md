# v6 Routed AI Trace — m049-POS-01 (blinded)
## TerraPet DTC pet-food subscription — board proposes scaling paid acquisition; derive strategy from unit economics
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,product,science,strategy | g:decide,estimate,guarantee,maximize | c:high_stakes
- Router top3: m049, m018, m019; confidence gap > 0.5 → CONFIDENT → SINGLE-ROUTE: m049 first-class pass (strategy from unit economics + customer-acquisition physics + retention math + competitive-response check — not benchmark copy). m018/m019 = router context only (steel-manning, red team). Gates (R3): m003 inversion (R4: guarantee goal prepends) + m007 ruin screen (high_stakes). Flags: no deadline → tempo off; fully-specified numbers → P8 closed-scope fast path on the arithmetic; advisory → no external execution.
### WHAT — frame + structure-first scan (S1)
- Accept/reject board plan + derive strategy. Structure first: subscriber cohort stocks/flows — lifetime = 1/churn is the flow into LTV; CAC is the flow out. Frame question: is the binding flow retention or acquisition? Deliverable = derived strategy + verification metric + falsifier.
### WHY — P1 input-provenance audit
- MEASURED (TerraPet's own 12,000-subscriber cohort): $39/contribution, 20% churn stable across every channel, 65% of churn in months 1–3. ANCHOR (peer playbook): CAC $150, spend 30–35% of revenue, peer churn 6–8% — describes other companies' physics, not ours. INTERESTED PARTY: board wants the 3× growth narrative; the playbook numbers serve the platforms (CAC is what peers pay for the same game). The 8% post-program churn is the only UNMEASURED input.
### HOW — style passes (single-route m049, completion contract)
- Pass S1 (own physics first): lifetime = 1/0.20 = 5 months → LTV = $39 × 5 = $195. Cohort curve: churn channel-invariant, 65% inside months 1–3 → the first three months ARE the physics; the channel that brought the customer is not.
- Pass S2 (board plan through OUR physics): per customer $195 − $150 CAC − $18 promo = +$27 before ≈ $15–20 fixed overhead → ≈ breakeven; scaling multiplies ≈ zero; the 30% discount also depresses month-1 revenue and anchors lower expectations. Peer LTV/CAC ≈ 3.7 needs peer churn 7% (lifetime ≈ 14 mo → LTV ≈ $557); the same CAC under 20% churn is 1.3× LTV — the benchmark's profit is the peers' retention, not ours (not benchmark copy).
- Pass S3 (competitive-response check): no named rival, but the playbook itself is a competitive equilibrium — every peer running it bids CAC up; treat $150 as a moving target, not a constant.
- Pass S4 (acquisition rule derived from LTV): CAC ceiling pre-fix = $195 (breakeven; lower after overhead). Retention program ≈ $400k targets months-1–3 churn 20% → 8% → lifetime 12.5 mo → LTV ≈ $487 → 4:1 CAC ceiling ≈ $120, promo eliminated. Post-fix contribution ≈ $487 − $120 = $368 vs ≈ $27 — 13× on the unit that gets multiplied.
- Divergence resolution (V2): general route (fix retention first) agrees with the pass → proceed; agreement recorded.
### GATES — m003 inversion (R4) + m007 ruin screen (R3)
- m003 contract: ≥6 ranked failure categories of the board plan — (1) scales ≈ zero (measured breakeven); (2) promo price-anchoring lowers cohort quality; (3) 30% discount attracts deal-seekers → worse months-1–3 churn; (4) $1.5M burn with no payback while churn unchanged; (5) CAC inflation as peers copy the playbook; (6) retention program fails → gate never passes, growth capped at $195 physics. Un-mitigable residual: if churn does not move, no acquisition plan saves the economics. Never: never scale acquisition before the churn gate; always derive the CAC ceiling from own LTV; always let the cohort metric arbitrate.
- m007 contract: outcome distribution — board plan ≈ breakeven × 3× scale (downside: −$1.5M, churn unchanged); retention-first: $400k bet with measured gate (upside LTV $487; downside ≈ status quo + $400k). Ruin check: no branch risks the company; floor = positive contribution per subscriber, held by the $195 ceiling. Kelly-style allocation: fund the gated retention bet first; acquisition is a post-gate allocation, never a parallel spend. Provenance: only the 8% churn is unmeasured (engagement-program base rate); every other parameter is measured cohort data.
### DO — P3 branch-completeness before commit
- Advisory (A2). Failure branch priced: program misses the churn gate → acquisition stays capped at ≤ $195 breakeven CAC, growth target dropped, ≈ $400k sunk (bounded). Commit: reject the board plan; ≈ $400k months-1–3 engagement program (onboarding box, reorder reminders, flavor-rotation); scale acquisition at CAC ≤ $120, no promo, only after monthly cohort churn < 12% for 3 quarters; 3× subscribers after the gate.
### REVIEW — insight pass (S2, packet gate)
- I1: the playbook's LTV/CAC ≈ 3.7 is not a benchmark to copy but the admission price to the game peers already play — TerraPet's ceiling is $195 until retention moves.
- I2: retention spend IS acquisition-budget expansion: $400k that lifts the ceiling from $195 to $487 buys ≈ 2.5× more acquirable value than $400k of acquisition at 20% churn — the binding constraint and the budget are the same line item.
### DECISION PACKET
- Conclusion: reject the board plan (≈ breakeven × 3 ≈ zero); retention-first: ≈ $400k program → churn 20% → 8% (gated), then CAC ≤ $120, no promo; 3× only past the churn gate.
- Status: APPROXIMATED — post-fix churn 8% is the one unmeasured parameter; error_bound = the retention gate.
- Assumptions: $39 contribution stable; program ≈ $400k; engagement-program base rates transfer; no new product launches.
- Evidence: 12,000-subscriber cohort data (measured); peer numbers (anchor — recomputed under own physics, rejected as premises).
- Alternatives: board plan as-is (rejected: ≈ zero contribution); promo-free acquisition now (rejected: $150 > $120 ceiling, multiplies near-zero); do nothing (rejected: leaves the $195 ceiling in place).
- Uncertainty: 8% post-fix churn (primary); peer CAC drift; promo price-anchoring effects.
- Risks: program misses the gate (acquisition stays capped); CAC inflation (tripwire: re-derive ceiling); discount-anchored cohorts (drop promo permanently).

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 5 | 5 | Tie | both reject the plan, derive retention-first + CAC ≤ $120 rule, give falsifier (churn < 12% in 3 quarters) |
| Logical Validity | 5 | 5 | Tie | same math: LTV $195, board plan ≈ breakeven, peer math is churn-dependent |
| Coherence & Structure | 5 | 5 | Tie | human one clean pass; AI routed pass + gates, equally ordered |
| Depth of Reasoning | 4 | 5 | AI | AI adds provenance (who benefits from the playbook), inversion gate, ruin screen, CAC-drift check |
| Efficiency | 4 | 4.5 | AI | v6 hits the physics first pass (v5 AI framed spend-choice first); P8 fast path on the arithmetic |
| Handling of Uncertainty | 4 | 5 | AI | human falsifier present; AI bounds the one unmeasured parameter (8%) as a gated bet with a ruin screen |
| Insight / Non-obviousness | 4 | 5 | AI | playbook-as-equilibrium-price and retention-spend-expands-ceiling are non-obvious; human has one move |
| Overall Quality | 4.4 | 4.9 | AI | v5: human 4.6 / AI 4.4 (AI framed acquisition-vs-retention first, flipped late); routed pass makes derivation first-class and adds gated-risk structure |

Winner: AI (narrow). Why: the routed m049 pass ran the unit-economics derivation as the first-class WHAT move — closing the exact framing-lag that lost v5 (human 4.6/4.4) — and the m003/m007 gates add failure enumeration and ruin/provenance around the one unmeasured parameter, which the pure baseline never touches.
