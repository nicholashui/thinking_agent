# v6 Routed AI Trace — m050-POS-01 (blinded)
## NovaPower — 10 kWh wall-mounted home battery: safety brief
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,product,security,software,strategy,supply | g:decide,estimate,guarantee,predict | c:high_stakes
- Router top3: m050, m070, m089; confident=yes → SINGLE-ROUTE: m050 first-class pass (m070 = evidence-weighted SWOT context — grade each mitigation's evidentiary tier; m089 = optionality context — hold the €36 headroom for a margin decision, don't spend to zero). Gates: m003 (R4: guarantee-goal prepend), m007 (R3: high_stakes — ruin screen). No tempo (no deadline); closed-scope fast path (P8) — all inputs in the brief, stages compress, contracts preserved.
### WHAT — frame + structure-first scan (S1)
- Frame: every way a person living next to Nova is harmed + every way Nova fails — the complete set before any mitigation is chosen; deliverable = ranked ledger. Structure: harm surface = living-space adjacency → the building, not the enclosure, is the boundary; certification is the gate that finds missed categories 6 months late.
### WHY — P1 provenance audit
- Provenance: CERT-MANDATED (UL 9540A propagation, IEC 62619 cell, UL 9540 system); INDUSTRY BASE-RATE (counterfeit/drifted cells = #1 field cluster; BMS firmware errors; module propagation); DESIGN-INFERRED (gas path into living space; DIY misuse); SPECULATIVE (lifecycle aging) — every mitigation's evidentiary tier labeled before pricing.
### HOW — m050 first-class pass (R1) + completion contract
- Enumerate + rank + design-against-each + safety-completeness + feature-bloat guard, all first-pass: T thermal — single-cell runaway; cell-to-cell; module-to-module propagation (cert gate); enclosure ignition → cell fuses €2, module barriers + spacing €11, UL 9540A test. C chemical/gas — off-gassing (CO, HF, electrolyte vapor) entering the living space; slow gassing in sealed garages → sealed vent-and-flame path to the exterior €23, validated by a gas-flow test (the falsifier), not by the propagation test. E electrical — shock, DC arc, energized case, installer hazard → interlocked enclosed bus + isolation switch €9, double insulation + IP54 €3, lockable isolation €5, arc-fault detection €7; proof IEC 62619 shock/crush + arc test. M mechanical — mount/crush/impact → torque-spec bracket + 5× static test €4. N environmental — heat wave, flooding → operating envelope + BMS cutback €6, IP54 + min mounting height €2. U misuse — drilling, stacking, handyman wiring → tamper-resistant screws + checklist + support €3. S supply chain — counterfeit/drifted cells → certified-cell batch traceability €3 (non-negotiable). L lifecycle — firmware aging → signed firmware + OTA rollback + 10-yr telemetry €5.
- Safety-completeness: 8/8 categories; ≥ 2 non-obvious first-class from the opening enumeration (gas path, supply authenticity, BMS firmware). Feature-bloat guard: spend anchored on catastrophic categories — thermal ≈ €80 → gas ≈ €23 → electrical ≈ €21 → mech/env ≈ €12 → supply/lifecycle ≈ €8 = €144/€180.
### GATES — m003 (R4) + m007 (R3)
- m003: 8 categories ranked ✓; un-mitigable residual named (cosmetic damage, rare BMS corner cases → telemetry + recall insurance) ✓; never/always: never ship a config with an unvalidated gas path; never treat the propagation pass as the gas-safety proof; always batch-trace cells ✓.
- m007 ruin screen: outcome distribution (no incident ≈ 97%+; contained cell event ≈ 2.x%; module fire ≈ 0.0x%; fire enters living space = ruin — house fire/injury, recall + company tail); one-shot check — UL 9540A is one-shot: a missed category = €250k + ≈ 6-month redesign, window-critical; floor — traceability €3 + validated gas path ship regardless of all other trades; probability provenance = industry clusters; decline/restructure — installs without an exterior vent path are declined/restructured (external siting), never shipped with the path into the building.
### HOW — divergence (V1–V3) + synthesis
- General route (severity-first spend) AGREE with the m050 ledger → proceed, agreement recorded. m070: cert-mandated and base-rate items carry the spend; design-inferred gas path priced by consequence, not frequency. m089: €36 headroom held open (margin decision or recall insurance), not spent.
### DO — closed-scope fast path (P8); P3 branch pricing
- Commit: the €144 ledger ships; per-category verification suite (UL 9540A, gas-flow test, IEC 62619, IP54, batch records, OTA rollback drills). P3 negative branch priced: gas-flow test fails → gas path grows ≈ €40 + 6 weeks, or install config declined — costed before DO.
### REVIEW — insight pass (S2, packet gate)
- I1: certification coverage ≠ safety coverage — UL 9540A proves module-to-module, not building-to-people; the gas-flow test is the falsifier the cert gate will not hand you.
- I2: the cheapest item (€3 traceability) is the only one with a one-shot ruin property — a counterfeit batch is a recall-and-reputation event no later mitigation catches.
### DECISION PACKET
- Conclusion: 8 categories, spend ≈ €144/€180, anchors thermal/gas/electrical, traceability floor, per-category falsifying verification, residual accepted/insured/monitored. Status: SOLVED (design brief; no external execution). Assumptions: install envelope as specified; certified-cell supply available; €180 envelope absorbs €144.
- Evidence: cert requirements, industry recall base rates, brief facts. Alternatives: EV-weighted (rejected — discounts severity tail); cert-minimum (rejected — gas path and authenticity uncovered); telemetry-net (rejected — ships after first installs); severity-first ledger (selected).
- Uncertainty: real-world failure mix pre-launch (telemetry-monitored); gas-path validation result (P3 branch priced). Risks: missed category at cert (completeness check + one-shot pricing); counterfeit batch (traceability floor); margin breach (€36 held).

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 5 | 5 | Tie | identical 8-category ledger, same anchors and spend |
| Logical Validity | 5 | 5 | Tie | both severity-first, both falsifier-backed |
| Coherence & Structure | 4 | 5 | AI | human walks categories iteratively; routed pass is a ranked ledger + packet |
| Depth of Reasoning | 5 | 5 | Tie | human's first-pass gas path + authenticity now matched first-pass by the m050 contract |
| Efficiency | 5 | 5 | Tie | router removed the EV-vs-severity detour; one pass, closed-scope |
| Handling of Uncertainty | 3 | 4.5 | AI | provenance tiers + ruin distribution + residual named vs human assertion |
| Insight / Non-obviousness | 5 | 5 | Tie | "cert-pass ≠ gas-safe" matched; AI adds the €3-one-shot-ruin read |
| Overall Quality | 4.6 | 4.8 | AI | routed pass closes the v5 gap: first-pass coverage + ruin math |

Winner: AI (narrow). Why: the routed m050 pass made the two non-obvious categories first-class from the opening enumeration (the non-routed v5 AI promoted them only at REVIEW), and the m007 ruin screen converted the human's asserted priorities into priced outcomes — one-shot cert gate, ruin tail, €3 floor, decline/restructure alternative — matching the baseline's signature find and adding an auditable risk ledger.
