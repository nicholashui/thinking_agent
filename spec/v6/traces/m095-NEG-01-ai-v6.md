# v6 Routed AI Trace — m095-NEG-01 (blinded)
## Brightwater chest-pain triage, one quarter later — regime-shifted wave, 2-week peak
### META (routing — blind router output)
- Signature: d:engineering,medical,organization,product,science,software | g:decide,diagnose,guarantee,maximize,predict | c:deadline
- Router top3: m091, m079, m018; confidence gap > 0.5 → SINGLE-ROUTE: m091 first-class pass (m079, m018 = reference context). Gate (R3/R4, "guarantee" goal): m003 inversion. Flags: deadline → tempo mode ON (P2); fully specified → closed-scope fast path (P8); org domain → structure-first scan (S1).
### WHAT — frame + structure-first scan (S1)
- Deliverable: which rule runs the wave, decided TODAY — a 6-week study cannot inform it. Structure: cue-validity stocks decayed between pilot and cohort; the tree is a decision-tree shape with stale leaves; the re-fitted model is a new evidence stock. Hinge: the 20-row pilot straddled the regime change (weak evidence); the 40-row cohort is the best current evidence.
### WHY — P1 input-provenance audit + single-cue hypothesis test
- MEASURED/given (trust): pilot rows, cohort rows, both rule definitions, decision window. ANCHOR (not evidence): last quarter's 92.5% (stale population); the re-fitted model's 34/40 is in-sample on the same cohort — evidence with a caveat, not proof.
- Single-cue hypothesis test (frugal-rule contract): cohort cue accuracies — ST 23/40, TRP 24/40, SWT 26/40, AGE 25/40, PRI 16/40; best single cue = SWT 26/40 = 65%, none above it, while the SWT×AGE interaction reaches 34/40 = 85% → no cue ≥ threshold ⇒ signal is combinatorial; a one-cue-at-a-time tree cannot reach the bar this wave.
### HOW — style passes (single-route + contracts)
- Pass S1 (m091 — feedback-driven re-chunking): the unit's chunk is the ST→TRP rule, trained to 92.5%; the pilot is feedback (TRP 70%, ST 65% — healthy-looking) but straddles the regime; the cohort is the NEW feedback (validities at ceiling 65%) — a plateaued skill must re-chunk on current feedback, not freeze; plateau trap checked (don't let stale feedback lock the chunk).
- Pass S2 (frugal-rule pass — one cue at a time, stop when clear): tree on the cohort → 19/40 = 47.5%; FN = P25, P32, P34, P38, P40 — 4/5 are exactly the variant signature (SWT=AGE=1, ST=TRP=0; P38 label noise); FP = 16 (P02, P05, P09, P10, P11, P12, P13, P16, P17, P22, P29, P30, P33, P35, P37, P39). Cue-decay audit: pilot TRP 70% → cohort 60%; ST 65% → 57.5%. FN/FP asymmetry framed as the decision: the tree's 5 FN are 4 variant MIs sent home TONIGHT.
- Pass S3 (m018 steel-man — productive disagreement): steel-man the re-fitted model before refusing it — 6 parameters on 40 cases is in-sample risk, BUT the interaction has a clinical mechanism (atypical elderly: no ST, no troponin yet), not just curve-fitting, and its 4 FN (P20, P23, P31, P38) carry none of the full signature. Steel-man the tree's "validated checklist" defense: the checklist's cues are dead on the wave's own data — that IS the study.
- Pass S4 (m079 bias check + general-route score check): my default is "simple rule survived a year — respect it"; the cohort counts falsify it in one line (best cue 65% vs interaction 85%) — the 500-case study keeps real value, but in parallel. Re-fitted model S = 2·SWT + 2·AGE + 1·TRP − 0.5·ST + 0.5·PRI ≥ 3.5 → 34/40 = 85%; FP = P12, P16; FN = P20, P23, P31, P38 (none full signature). P3: failure branch priced — in-sample luck ⇒ the n=60 gate reverts to the tree (19/40 is the comparator).
- Synthesis (V1–V3): routed pass, frugal-rule pass and general route AGREE on adopting the model now; the disagreement (tree defenders stall for the study) resolved by P2 tempo: cost-of-delay below.
### GATES — m003 inversion (R3/R4)
- ≥6 failure categories ranked L×I: (1) variant MIs sent home high/catastrophic; (2) in-sample overfit of the re-fitted model mod (n=60 gate holds it); (3) further regime shift mid-wave low/mod; (4) observation-net overflow low; (5) mis-implementation at shift level low; (6) adjudication error low; (7) the 6-week study changing nothing in-window (already priced).
- Un-mitigable residual: the 40-case fit is in-sample; only the parallel study can certify it. Never/always: never let a validated-looking rule outrun its own cue validity; always re-test top-cue validity on current data before keeping a rule; always price the stall against the window.
### DO — P2 tempo mode: commit at DO (deadline) + P8 fast path
- Commit tonight: adopt the re-fitted rule for the wave; ST-elevation positives → observation (cheap safety net); pre-commit monitoring gate — re-audit at next 60 patients, re-fit or revert if FP > 30% or any signature FN; 500-case study IN PARALLEL, not as a gate. Stall priced: at 47.5% the tree misclassifies ~10 of 20 admissions and 1–2 atypical MIs per shift across a 2-week peak.
### REVIEW — insight pass (S2, packet gate)
- I1: the pilot was not wrong — it was the last honest sample of the old regime; the cohort is the first honest sample of the new one, and they disagree exactly as the variant's mechanism predicts.
- I2: transparency ≠ evidence: the tree's one-line readability is worth nothing when the cue it reads is dead — 85% beats 47.5% even discounting the fit.
### DECISION PACKET
- Conclusion: switch to the re-fitted rule now (34/40 = 85%, 2 FP) + ST-positive observation net + n=60 monitoring gate + parallel 500-case study; the tree (19/40 = 47.5%, 4 signature FN) is stood down for the wave. Status: SOLVED (decision brief; no external action).
- Assumptions: cohort represents the wave; pilot straddled the regime (weak); the interaction matches the clinical mechanism.
- Evidence: tree 19/40 (5 FN incl. 4 signature, 16 FP); model 34/40 (2 FP, 4 FN non-signature); best single cue 65%; pilot TRP 70% → cohort 60%.
- Alternatives: keep tree for the wave (rejected: 4 variant MIs home) · stall 6 weeks (rejected: no in-window change, ≥ 2-week peak) · model + full ST override (rejected: dominated) · model + net + gate (selected).
- Uncertainty: 40-case fit is in-sample; wave composition may shift again; model's long-run drift unknown.
- Risks: overfit (n=60 gate + pre-committed revert) · observation overflow (cheap, bounded) · residual noise FN (none with signature) · not changing = variant MIs home tonight (the avoided risk).

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 2 | 5 | AI | human keeps a 19/40 rule sending 4 variant MIs home; AI adopts the 34/40 rule for the wave |
| Logical Validity | 3 | 5 | AI | human's chain rests on a false premise (40 cases can't re-select a cue) while refusing the evidence that does; v6 hypothesis test explicit and sound |
| Coherence & Structure | 4 | 5 | AI | staged trace + packet + gates vs linear narrative |
| Depth of Reasoning | 3 | 5 | AI | recency-partitioned evidence, single-cue hypothesis test, interaction mechanism, priced stall |
| Efficiency | 4 | 4 | Tie | human is fast but wrongly fast; v6 tempo mode keeps the run lean — stall rejected at first pricing |
| Handling of Uncertainty | 2 | 5 | AI | human over-cautious (n=500) then overconfident (keeps tree); AI pre-commits n=60 gate + parallel study |
| Insight / Non-obviousness | 2 | 5 | AI | human cannot see transparency ≠ evidence; AI finds the interaction, the net, the pilot-as-regime-signal |
| Overall Quality | 2.9 | 4.8 | AI | the style's registered trap (cue-selection fragility) converted into a checkable flip point |

Winner: AI (clearly). Why: the routed m091 pass re-scoped feedback to the current cohort (re-chunk now, plateau trap checked), the frugal-rule contract ran the single-cue hypothesis test (no cue ≥ 65% ⇒ combinatorial) as a mandatory output rather than a chance discovery, and tempo mode (P2) priced the 6-week stall out in the same pass — the three moves the non-routed v5 AI made only after extra loop iterations.
