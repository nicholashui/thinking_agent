# v6 Routed AI Trace — m041-POS-01 (blinded)
## First customer conference — 3 days / 600 attendees / 40 speakers / 25 sponsors / 12 weeks / 4 FTE / $150k
### META (routing — blind router output)
- Signature: d:finance,medical,product,strategy | g:guarantee,predict | c:high_stakes
- Router top3: m024, m039, m041; confidence gap ≤ 0.5 → AMBIGUOUS → DUAL-ROUTE: m024 + m039 first-class passes, synthesized (m041 = synthesis context). Gates (R3): m003 inversion + m007 ruin screen. Flags: one-shot event (m007 one-shot check live); P8 closed-scope fast path (fully specified; deliverable = internal plan); no tempo flag (no deadline).
### WHAT — frame + structure-first scan (S1)
- Frame: a complete, feasible, resource-consistent plan claiming EVERY clause of the goal (3 days · 600 · 40 · 25 · 12 wks · 4 FTE · $150k), nothing left to the event's last week. Structure first: the shape is a dependency network — one external gate (venue, 3–6-month lead) feeding save-the-date → registration → demand data → logistics; two income floors (registration, confirmed sponsorship); one fixed resource budget (4 FTE).
- Completeness contract: every goal clause → exactly one level-1 stream, mapped BEFORE deepening (clause-to-stream matrix is a WHAT gate artifact, not a REVIEW sweep).
### WHY — P1 input-provenance audit
- GIVEN (trust): 4 FTE, $150k, 12 wks, target counts. MEASURED-RISK: venue market competitive (3–6-month lead) — the only hard external probability in the plan. ANCHOR: none — first-ever event, no agency, generalist staff → the plan must be self-contained; no experience-memory to inherit.
### HOW — style passes (dual-route, synthesize)
- Pass S1 (m024 regret minimization — long-horizon clarity): at 80, the regrets are "conference failed publicly" (no venue, safety incident, no-show chaos) and "never held it" — the first dominates → plan ordered by FAIL-SAFE ordering: highest-regret nodes locked first (venue contract, safety/emergency, catering floor), not by glamour (speakers).
- Pass S2 (m039 asymmetric upside / barbell): reject the symmetric middle bet (even spend everywhere → nothing guaranteed). Safe core: venue w/ 650+ hold, registration system, catering floor, run-of-show, safety — funded and scheduled with margin. Upside: premium speakers, sponsor upsells, extra tracks — funded ONLY from confirmed sponsor revenue, after core locks. Budget built bottom-up from the two guaranteed floors; contingency is the only unallocated money.
- Synthesis (m041 decomposition — completion contract): 5 streams from goal clauses — S1 venue/logistics ← "3 days/600"; S2 content/speakers ← "40"; S3 marketing/registration ← "600"; S4 sponsorship/finance ← "25/$150k"; S5 on-site ops ← "3 days × 600" — 5/5 clauses claimed first pass. Recurse to leaves (owner class + ≤ 2 wks: "contract venue + AV in writing by wk 4"). 7 dependency edges; critical path venue (wk 4) → save-the-date (wk 4) → registration live (wk 5) → demand data (wk 8) → logistics finalize (wk 10) → run-of-show (wk 11) = 10 wks, 1 wk float, float created by the enforced wk-7 speaker-confirmation deadline. 4 FTE ↔ 4 concurrent streams (S5 folds into COO after wk 8). Merge-back designed: single source of truth, budget roll-up w/ 10% contingency at wk 11, minute-by-minute run-of-show. Cross-cutting check (decomposition-blindness guard): venue availability spans S1/S3 (backup track), accessibility/emergency spans S1/S5, sponsor revenue spans S4→S1/S3 — none owned by one stream.
- Divergence (V1–V3): passes AGREE — regret structure = fail-safe ordering; barbell = core protection + revenue-floor budget; decomposition = the machinery both imply → proceed, agreement recorded.
### GATES — m003 inversion + m007 ruin screen (R3)
- m003: ≥6 failure categories ranked L×I: (1) no venue at 12 wks (med/catastrophic — external, non-compressible); (2) speaker confirmations miss wk-7 deadline → agenda collapse (med/high); (3) registration volume ≠ room/food (med/high); (4) sponsor shortfall → budget breach (med/med-high); (5) safety/emergency unprepared (low/catastrophic); (6) 4 generalist FTE under on-site load (med/high); (7) accessibility gaps (low-med/med-high); (8) critical-path slip cascade (low/high). Un-mitigable residual: external acceptances (venue, speakers, sponsors) — mitigated, not eliminated. Never/always: never open registration before the venue contract is signed; always backstop the critical path; always price the failure branch before selection.
- m007: ONE-SHOT — single event, no do-over; reputational damage irreversible → ruin branch dominates EV. Outcome distribution: P(complete, on-time, ≤ budget) 0.70; P(on-time, contingency draw ≤ $165k) 0.20; P(shrink: venue change → 450 + virtual track) 0.08; P(ruin: no venue / major incident) 0.02. Floor: if P(venue) < 0.85 by wk 4 → trigger backup track + restructure decision (1-day briefing + virtual). Provenance: venue availability is the only externally-sourced probability; the rest is internal discipline. Decline/restructure alternative: 1-day executive briefing + virtual track — named, not triggered.
### DO — P8 closed-scope fast path + P3 branch completeness
- Deliverable is a plan (internal action). Commit: 5-stream hierarchy as designed; critical path 10 wks + 1 wk float; wk-4 go/no-go on venue; 10% contingency; backup venue track running from wk 2. Failure branch priced above — no unbudgeted state reachable.
### REVIEW — insight pass (S2, packet gate)
- I1: the venue is the only goal clause whose risk is external and non-compressible — the real critical path is "venue → save-the-date → registration → demand data," and the only float source is the enforced wk-7 confirmation deadline.
- I2: at 600 attendees with 4 generalist FTE, the binding on-site load is the day itself — the run-of-show must draft supplier day-of staff into staffing, or the barbell breaks on the last day.
### DECISION PACKET
- Conclusion: 5-stream, 3-level plan; all 5 goal parts ≥2 actionable leaves on the FIRST pass; 7 edges; critical path 10 wks + 1 wk float; 4-FTE mapping; designed merge-back; ruin floor with decline alternative. Feasible in 12 wks.
- Status: SOLVED (fully specified internal deliverable; completeness gate passed first pass; no external action). Assumptions: venue obtainable by wk 4 (backup priced); wk-7 deadline enforceable; revenue floors as budgeted.
- Evidence: clause-to-stream matrix 5/5; leaves (owner + ≤2 wks); edge list; path/float arithmetic; ruin distribution; inversion categories 1–8.
- Alternatives: A flat checklist (rejected — no ordering/ownership) · B timeline-first (rejected — no resource map) · C hierarchy + gates (selected) · D event agency (rejected — out of scope).
- Uncertainty: venue timing ±1 wk on critical path; registration response curve; speaker attrition post-wk-7. Risks: 0.02 ruin branch (floor trigger armed); 0.20 contingency draw; sponsor payment timing; resource contention with daily duties (weekly sync).

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 5 | 5 | Tie | both complete and feasible; AI's clause-to-stream gate makes completeness first-pass, not second-sweep |
| Logical Validity | 5 | 5 | Tie | identical dependency logic, edge list, critical-path arithmetic |
| Coherence & Structure | 4 | 5 | AI | dual-route + gates + packet vs linear build-up; merge-back designed, not reviewed-in |
| Depth of Reasoning | 5 | 5 | Tie | AI matches 3 levels / 7 edges / path+float and adds inversion + ruin layers |
| Efficiency | 4 | 5 | AI | no second coverage sweep — completeness is a gate artifact, not a REVIEW discovery |
| Handling of Uncertainty | 5 | 5 | Tie | both bake float + contingency; AI adds one-shot ruin floor with a wk-4 trigger |
| Insight / Non-obviousness | 4 | 5 | AI | revenue-floor barbell; venue as the only non-compressible external risk; go/no-go wk-4 |
| Overall Quality | 4.8 | 4.9 | AI | AI narrow |

Winner: AI (narrow). Why: the routed m041 completion contract (clause-to-stream coverage gate, critical path + float in design, merge-back first-class) closes exactly the three gaps the v5 baseline won on, and the m007 ruin screen converts venue risk from a REVIEW note into a governed floor trigger.
