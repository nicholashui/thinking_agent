# v6 Routed AI Trace — m066-POS-01 (blinded)
## Aurora Cells — Q3 swelling jump 3.1×; two co-shipped week-37 changes, zero overlap; recall $4–6M
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,product,science,security,software,supply | g:estimate,guarantee,maximize | c:deadline
- Router top3: m066, m031, m070; confidence gap > 0.5 → CONFIDENT → SINGLE ROUTE: m066 first-class pass (m031, m070 = context). Gate (R3/R4, guarantee): m003 inversion. Tempo mode ON (P2, deadline). Closed-scope fast path (P8) REJECTED — deliverable is a design + decision protocol under constraints, cause unknown, not fully specified.
### WHAT — frame + structure-first scan (S1)
- Frame: causal attribution under perfect collinearity — the ONLY decisive path is manufacturing the missing cells (an experiment), not re-reading history. Structure: decision-tree shape with 5 outcome branches → inference map; cost asymmetry $3.1k experiment vs $4–6M recall.
### WHY — P1 input-provenance audit
- MEASURED: 3.1× jump, week-37 co-ship of (anode switch) and (formation retune); zero cells off-diagonal; lots E-77/E-88 overlap the jump; reefer outage on 3 routes ≤ 36 h. ANCHOR / INTERESTED-PARTY: "CarbonCore is the cause" — the visible event; the narrator (production log, supplier story) benefits from a fast attribution; the silent profile change needs no narrator. Salience is inverse evidence.
- Hypotheses with falsifiers (P6): H1 anode · H2 formation profile · H3 electrolyte lot · H4 transport temperature · H5 interactions. Collinearity stated: old-anode+new-profile and new-anode+old-profile exist in ZERO cells → any retrospective regression splits blame arbitrarily → structurally powerless.
### HOW — style pass (S1 first-class, completion contract: ≥2 competing hypotheses + discriminating experiment designed + single-hypothesis-fixation guard)
- 4 hypotheses held simultaneously, none privileged by salience; confound named as the decisive feature.
- Experiment: 2×2 factorial (anode × profile) × 30 cells/arm = 120, built from remaining AnodeTech + current CarbonCore stock, same week, lot-blocked (E-88; E-77 flagged separately), 45 °C / 2-week screen; ≈ 120 × $26 ≈ $3.1k ≪ $4–6M; interim read at 2 weeks (tempo, 6-week deadline).
- Outcome→inference map, complete: CarbonCore-high in both profiles → H1; new-profile-high in both anodes → H2; both main effects, no interaction → H1+H2 additive; high ONLY in CarbonCore×new → interaction — revert either factor alone fixes it (a different decision than H1+H2); all four arms high incl. old×old → ambient cause → second-stage: lot split × shipping-route cross-check (lot tracks → H3; route tracks → H4).
- Traps the design kills (contract guard): single-hypothesis fixation on the salient supplier change; uncontrolled whole-population rollback ("revert the profile and watch") — no counterfactual arm, attributes nothing.
### GATES — m003 inversion (R3)
- ≥6 failure categories ranked L×I: (1) fix on CarbonCore, wrong factor rolled back — persistence + recall (H/H); (2) uncontrolled profile rollback — week lost, nothing attributed (H/H); (3) retrospective regression on collinear data — spurious attribution, false confidence (H/H); (4) power thin if effect < 3× — false null, recall (M/H); (5) screen doesn't reproduce customer-visible swelling — silent failure (M/H); (6) AnodeTech stock shortfall (M/M); (7) interim-dominant read stops the run early — interaction missed (M/M); (8) 6-week slip → recall decision forced without data (M/H).
- Un-mitigable residual: customer reports may lag production by weeks — screen verdict may not map cleanly to shipped-cell exposure. Never/always: never attribute from collinear history; always manufacture the missing cells; never roll back a whole population without a counterfactual arm; always pre-commit the outcome→action map before the run.
### DO — tempo commit (P2) + P3 branch pricing
- Commit: run the factorial as the decision protocol; every branch priced incl. failure branch — null outcome → customer-failure-mode study + escalate, bounded at ≈ 1 more screen cycle within the 6-week horizon. Divergence (V1–V3): m066 vs general route AGREE (prospective factorial = only decisive path) → V2 recorded, proceed.
### REVIEW — insight pass (S2, packet gate)
- I1: salience is inverse evidence — the visible change is the least trustworthy suspect because it needs no experiment to be believed; the silent change is the one history would never surface.
- I2: the experiment is negative-cost against the liability — $3.1k buys decision rights over a $4–6M exposure; information is cheaper than any guess here.
### DECISION PACKET
- Conclusion: run the 2×2 factorial, 30/arm, lot-blocked, with the full outcome→inference map as the pre-committed decision protocol (incl. the interaction branch: revert either factor alone); 2-week interim read; ≈ $3.1k.
- Status: NEEDS_EXPERIMENT (probe available — factorial designed; execution is the external action, verification at the 2-week interim read). Assumptions: AnodeTech stock available/representative; screen reproduces customer-visible failure; 3.1× is a real signal.
- Evidence: production logs (week-37 co-ship, zero overlap); QA log (lots, reefer outage); screen protocol; cost data. Alternatives: A retrospective deep-dive (rejected — powerless under collinearity) · B factorial (selected) · C uncontrolled rollback (rejected — pseudo-experiment) · D lab aging simulation (rejected — misses line interaction + transport).
- Uncertainty: true effect size (sized 3×; scale on interim variance); screen fidelity; customer-report lag. Risks: false null → escalation plan; stock shortfall → block design uses AnodeTech only in two arms; interim dominant read → interaction check before stopping.

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 5 | 5 | Tie | identical factorial + full outcome map |
| Logical Validity | 5 | 5 | Tie | same elimination logic; interaction branch drawn by both |
| Coherence & Structure | 4 | 5 | AI | routed pass + inversion gate + tempo-bound protocol vs linear walk |
| Depth of Reasoning | 5 | 5 | Tie | outcome map now a mandatory contract output (v5 gap closed); P6 falsifiers per hypothesis |
| Efficiency | 5 | 5 | Tie | P3 collapses the rejected alternatives (retrospective/rollback/lab) into one priced branch (v5 E=3.0 gap closed) |
| Handling of Uncertainty | 3 | 4.5 | AI | power/stock/fidelity/lag residuals + interim read; human asserts power |
| Insight / Non-obviousness | 5 | 4.5 | Human | human's first-pass trap-naming still sharpest; AI adds salience-is-inverse-evidence + negative-cost experiment |
| Overall Quality | 4.6 | 4.9 | AI | correctness tied; routed pass turns the baseline's winning moves (map + traps) into contract outputs and adds the decision protocol |

Winner: AI (narrow). Why: the routed first-class pass installs the pure baseline's signature moves — hold all hypotheses, find the collinearity, manufacture the missing cells, map every outcome to elimination — as mandatory completion-contract outputs plus an m003 inversion gate, P3-priced failure branches, and a tempo-bound interim read; the baseline retains only a stylistic first-pass edge, so the win is narrow, not a rout.
