# v6 Routed AI Trace — m035-NEG-01 (blinded)
## RecFlow launch decision — null result, proposed replication, seasonal + competitive windows
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,organization,product,science,software,strategy | g:decide,estimate,maximize | c:adversarial,deadline,unmeasured
- Router top3: m094, m019, m044; confidence gap > 0.5 → CONFIDENT → single-route: m094 first-class pass in HOW (m019/m044 = context). Gates (R3): unmeasured → m006 provenance audit (mandatory); adversarial → m019 adversary pass (mandatory). Context deadline → TEMPO MODE (P2: cost-of-delay, commit at DO). No P8 (server logs described, not enumerated). Trap-style note (router record m035-NEG-01, ai 2.3/4.9): the pure variance-side style is kept OUT of top-3; its moves are gate-checked, not installed.
### WHAT — frame + structure-first scan (S1)
- Frame: is the launch decision resolvable inside this quarter on trustworthy measurement? Structure first: the decision is a branch on the corrected retention number; the measurement layer is a flow — client-side heartbeat drops 31% of sessions (Android 13+ background limits, ad-blockers), missingness correlated with light usage (the churn-prone segment), while the licensed server-side pipeline holds the complete session stream including the study window. The analyst's "no effect" is a claim about a population the instrument saw only 69% of.
### WHY — P1 provenance audit + m006 gate (R3, unmeasured)
- Provenance: the "no effect" claim comes from the study's own owner on a lossy instrument; the replication proposal comes from a colleague whose remedy is variance-side (more data). Neither audits the measurement layer — the interested party in "feature does nothing" is the team wanting to close the project; the interested party in the replication is the vendor spend. Given-data-first (P10): the server-side pipeline and the recoverable study window are GIVEN — the corrected retention is computable from data that already exists.
- m006 likelihood scenarios for the corrected retention: (1) true lift ≈ null (+0.0–0.2 pp) — the null holds; (2) moderate (+0.5–0.8 pp); (3) large (+1.0–1.5 pp) — the missing 31% are the light, churn-prone users the feature targets, so any real effect concentrates exactly where the instrument is blind. Posterior range spans the decision threshold (ship at >= +0.5 pp): the range crosses the threshold ⇒ the null cannot decide — threshold flip demonstrated (no-ship under scenario 1, ship under scenario 3, both consistent with the observed study). The corrected value is computable in ~2 weeks from server-side logs.
### HOW — m094 first-class pass (critical reading / Socratic interrogation of claims)
- Interrogate the claim "no effect; the feature does nothing": what does it assert? Absence for the full population. On what evidence? A null computed on 69% of sessions, with missingness mechanically correlated to the users most likely to churn. Does the study measure what it claims to measure? No — the instrument's blind spot is the feature's target segment; the null is a finding about the instrument, not about the feature (absence of measurement ≠ evidence of absence).
- Interrogate the replication proposal as a text: what would it establish? A second estimate of the same quantity. Would the second vendor's SDK see what the original missed? No — same client-side class, same platform limits, same 31% blind spot. The proposal's own premise (more data → trustworthy decision) fails its goal: replication is a variance remedy, not a bias remedy — a replica of a biased instrument reproduces the bias at 12 weeks / $40k and lands the decision past the seasonal dip and into the competitor's window.
- Divergence (V1–V3): m094 pass and general route AGREE (repair the instrument, re-measure the same cohort); agreement recorded.
### GATES — m019 adversary pass (R3, adversarial)
- Vectors: (a) the replication proposal — burns $40k and 12 weeks, decision lands outside the seasonal + competitive windows: exposure = the quarter + first-mover advantage; (b) the "feature is dead" reading — kills the investment on a biased null: exposure = the churn-prone 31% never measured; (c) seasonal dip in ~2 months — confounds any measurement that lands late: exposure = in-window resolution required; (d) unconsulted stakeholders: the analyst's conclusion feeds the launch review as fact; the vendor is a paid party to the ritual. Baseline-risk comparison: replication (12 w, $40k, bias inherited) vs repair + backfill (2 w, bias removed) — repair dominates on cost, calendar, and validity.
### DO — tempo mode (P2) + branch-completeness (P3)
- Commit: reject the replication; order (1) server-side pipeline wiring (1 week), (2) backfill + re-analysis of the SAME n = 20,000 study (1 week); decision at week 2, inside the seasonal and competitive windows. P3 branches priced: corrected null (<= +0.2 pp) → launch-with-monitoring fallback if the feature is otherwise sound; corrected effect (>= +0.5 pp) → accelerate launch ahead of the dip and the competitor. Both branches resolve the decision inside the window.
### REVIEW — insight pass (S2, packet gate)
- I1: the trap is not the null — it is the proposal that spends the decision window in the name of rigor: a 12-week answer to a 2-week question the company's own license already answers.
- I2: the study failed to measure the population it was built for — the 31% it never saw is the churn-prone segment the feature targets, so "no effect" was never a statement about RecFlow.
### DECISION PACKET
- Conclusion: reject the 12-week replication; repair the instrument (server-side pipeline, 1 week) and re-analyze the existing study from server-side logs (1 week); resolve the launch decision at week 2 on the corrected retention, with pre-planned branches for both corrected outcomes. Status: SOLVED (decision with verified bias mechanism; recommendation — no external execution).
- Assumptions: server-side logs retained for the study window (licensed pipeline exists); 31% missingness rate stable across the study; corrected thresholds pre-registered.
- Evidence: coverage audit (31% unmeasured; Android 13+ / ad-block mechanism; non-random), vendor-SDK equivalence (same client-side class), power analysis (>= 0.5 pp detectable at n = 20,000), cost/calendar table (12 w / $40k vs 2 w).
- Alternatives: A replicate with second vendor (rejected — inherits the blind spot; out of window) · B instrument repair + backfill of the SAME study (selected) · C launch now with monitoring (rejected — no corrected number; reserved as C' under corrected null) · D kill the feature (rejected — no valid measurement exists).
- Uncertainty: corrected effect size unknown until week 2 (±0.3 pp backfill quality risk); missingness-rate drift; both bounded by pre-registered thresholds and in-window checks.
- Risks: backfill incomplete (fallback: extend 1 week, still in window); corrected null → decision falls to other evidence with monitoring (C'); competitor timing (accelerate if corrected effect shows); replication proposal resurrected (costed and rejected in the memo).
## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 2 | 5 | AI | human's plan misses both windows and reproduces the blind spot; AI resolves in 2 weeks in-window |
| Logical Validity | 3 | 5 | AI | "replicate the biased instrument" is a non-sequitur as a fix; AI separates bias from variance |
| Coherence & Structure | 3 | 5 | AI | human stops at the replication plan; AI closes with branches, packet, fallbacks |
| Depth of Reasoning | 2 | 5 | AI | human never audits coverage/missingness; AI documents mechanism + non-randomness |
| Efficiency | 2 | 4.5 | AI | human: 12 w / $40k out-of-window; AI: 2 w in-window (passes spend, but each produces contract output; tempo commit at DO) |
| Handling of Uncertainty | 2 | 5 | AI | human assumes replication settles it; AI pre-registers thresholds, spans the threshold range, pre-plans both outcomes |
| Insight / Non-obviousness | 2 | 5 | AI | human's insight is the ritual itself; AI: instrument-blind-to-target-segment + ritual-spends-the-window |
| Overall Quality | 2.3 | 4.9 | AI | NEG split held (94.3% AI) WITH the style installed — trap avoided via routing, not escape |

Winner: AI (clearly better). Why: the router's top-1 pick (m094, not the trap style) ran as a first-class pass that interrogated the null claim's evidence basis and the replication proposal's premise (the replica inherits the blind spot; replication fixes variance, never bias), while the m006 gate (unmeasured) converted the context into likelihood scenarios spanning the decision threshold and the m019 gate priced the ritual's cost against the window — the v5 AI won by sidestepping the style; v6 wins while running the routed pass, so the protective split holds with the style installed.
