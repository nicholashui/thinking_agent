# v6 Routed AI Trace — m060-POS-01 (blinded)
## Selection-process design for 12 field-technician hires (design brief + cost justification)
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,product,science,software | g:diagnose,estimate,maximize,predict | c:(none). Router top3: m060, m015, m018; confident=yes → SINGLE ROUTE (no G1 dual-route): m060 = first-class pass in HOW (R1), m015 + m018 = supporting context passes. No context-triggered gates (no deadline/high_stakes/adversarial/unmeasured). R4 maximize-goal → falsifiable checkpoint required. No deadline → tempo mode off.
### WHAT — frame + structure-first scan (S1)
- Frame: deliverable = signal ranking + scoring/weighting scheme + decision rule + cost-vs-expected-return justification. Structure-first (org/finance/product domains): the decision is a betting structure — signals as predictors, composite as the bet, turnover as the loss stream; at ~100 applicants for 12 hires the base rate dominates the average draw. The old process's implicit weights are the structure to fix, not the candidates.
### WHY — P1 provenance audit (measured vs anchor vs interested-party)
- MEASURED: the org's own study (n=120, org-calibrated r values), documented costs ($18k bad hire, $6k training, $40 budget). ANCHOR: the ops lead's "do I get a good feel?" is anchor language, not evidence; the old process's de facto validity ≈ 0.28 is a conversational weighting. INTERESTED-PARTY: the ops lead owns the incumbent 30-minute interview — the process is his instrument; who benefits from keeping it. P10 given-data-first: the supplied validity table is the ordering authority — no re-derivation in WHY (the ranking is decided the moment the numbers are given).
### HOW — style passes (single route, completion contracts)
- Pass S1 (m060, first-class — contract: signals ranked by predictive validity from base rates, work samples > structured interviews > interviews > tests; process designed around top signals; candidate-specific adjustment justified against an evidence bar): rank 0.52 > 0.47 > 0.21 > 0.17 > 0.13 → weights by validity share 50/30/10/10; unstructured interview 0% of score (r = 0.21 = interview theater — 30 conversational minutes measuring how much the interviewer likes the person, the exact producer of the 45% turnover); references/experience demoted to pass/fail red-flag screens (criminal, falsified employment, safety), never score drivers; decision rule composite ≥ 70/100, work-sample tie-break, override only for documented superior specific evidence (authenticated job-identical artifacts) — the evidence-gated exception, so the gut cannot climb back through the exception path.
- Pass S2 (m015 — complexity scan): 45% turnover is an emergent property of a personality-weighted process at volume, not a talent-pool problem; emergent risk of the new design: mechanical-rule erosion one manager at a time — the rule must be instrument, not opinion.
- Pass S3 (m018 steelman — contract: opposing position rebuilt in strongest defensible form BEFORE critique, survivor test): STEELMAN the old process ("the interviewer sees things the numbers can't; a 20-year veteran knows the territory; the work sample is a snapshot"). CRITIQUE from the study only: r = 0.21 is the measured ceiling of the room; r = 0.13 means a 20-year veteran is just experienced at failing; the work sample at r = 0.52 beats the room by 2.5×. Deepest objection ("mechanical rules miss context") answered: the override clause IS the context channel — gated on evidence, not narrative.
- Divergence resolution (V1–V3): all three passes and the general route AGREE on the validity-weighted design — agreement recorded, no calibration pass needed.
### GATES — R4 falsifiable checkpoint (no R3 triggers on this signature)
- Falsifier: refit the r values on actual hire-outcome tracking within 12 months — if the work sample's r collapses below the structured interview's on Meridian's own data, the weights are re-derived from the new table; the process is the hypothesis, outcome data is the judge.
### DO — P3 branch completeness + closed-scope fast path (P8)
- Fully specified (validity table, costs, budget given) → stages compressed. Negative branch priced: "new process under-delivers" → annual re-validation with outcome tracking; "old process kept" → 4–5 bad hires ≈ $72–90k. Verdict committed: weights 50/30/10/10, screens for references/experience, unstructured out of scoring, threshold ≥ 70, work-sample tie-break; cost 22+8+2 = $32/applicant ≤ $40.
### REVIEW — insight pass (S2, packet gate)
- I1: the budget cap is not a constraint — it is the cheapest insurance in the process: $3.2–3.4k of assessment to avoid ≥ $36–54k of bad hires (validity 0.28 → 0.50 roughly halves the bad-hire rate).
- I2: the interview measures the room, not the job — when they disagree, the room lies; the work sample IS the job.
### DECISION PACKET
- Conclusion: adopt the validity-weighted process — work sample 50% / structured 30% / references+experience as screens only / unstructured 0%; composite ≥ 70 with work-sample tie-break; evidence-gated override; expected validity ≈ 0.50 vs old ≈ 0.28 → ≈ 2 bad hires of 12 instead of 4–5, ≈ $36–54k avoided against ≈ $3.2–3.4k assessment.
- Status: SOLVED (design delivered, alternatives verified, no external action). Assumptions: study (n=120) generalizes to new territories; ~100 applicants for 12 hires; pool quality unchanged; managers comply with the mechanical rule.
- Evidence: org validity study (0.52/0.47/0.21/0.17/0.13); turnover 45% + bad-hire $18k; old de facto validity 0.28. Alternatives: B culture-fit weighting (rejected — r=0.21 re-enters the score) · C cheap screen (rejected — validity ≈ 0.20, 10× the assessment cost in bad hires) · D work-sample only (rejected — single-signal measurement error) · A validity-weighted composite (selected).
- Uncertainty: r ± ~0.1 at n=120; pool quality pre-launch; compliance risk with a mechanical rule. Risks: charismatic hire slips through (mitigated: mechanical threshold + tie-break + logged, evidence-gated deviations); validity drift (mitigated: annual re-validation, falsifiable checkpoint); false negatives on unconventional candidates (mitigated: the override clause routes superior specific evidence around the cutoff, documented).

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 5 | 5 | Tie | both hit all 7 rubric points: ranking with numbers, work sample ≥ 45%, unstructured excluded, screens, explicit rule, quantified uplift, interview-illusion named |
| Logical Validity | 5 | 5 | Tie | same validity logic; AI adds the evidence-gated override clause as the justified candidate-specific adjustment |
| Coherence & Structure | 4 | 5 | AI | human is a linear walk; AI has routed passes + falsifiable checkpoint + packet |
| Depth of Reasoning | 5 | 5 | Tie | human's "the room lies" framing; AI adds P1 provenance (anchor vs measured) + steelman of the old process + emergent turnover scan |
| Efficiency | 5 | 5 | Tie | table-first on both sides; AI's P10 given-data-first + P8 fast path remove the v5 re-derivation stage |
| Handling of Uncertainty | 3 | 5 | AI | human asserts; AI names r ± ~0.1 CIs, falsifiable checkpoint, compliance risk, re-validation |
| Insight / Non-obviousness | 4 | 5 | AI | human's screens-not-drivers kill; AI adds "budget cap = cheapest insurance" and "the room lies" via the steelman pass |
| Overall Quality | 4.4 | 4.7 | AI | the v5 gap (AI 4.1: re-derived base rates in WHY, insight 3) is closed — same design, first-pass anchoring, extra auditability |

Winner: AI (narrow) vs v5 human baseline. Why: the routed m060 first-class pass installed the table-first base-rate move as the opening — weights from validity, low-validity signals demoted to screens, override evidence-gated — fixing the v5 run's efficiency loss (P10 given-data-first, P8 fast path) and its missed framings (insight pass I1/I2, m018 steelman of the old process), while every rubric point and the decision match the human baseline.
