# v6 Routed AI Trace — m097-NEG-01 (blinded)
## Project Curlew: forecast + plan + response to the consultant vs a 6-week/$110K inside plan
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,organization,product,software,strategy,supply | g:decide,estimate,predict | c:(none)
- Router top3: m044, m070, m075; confidence gap ≤ 0.5 → AMBIGUOUS → DUAL-ROUTE: m044 + m070 first-class passes, m075 synthesis context (satisficing: internal deliverable, sufficient evidence, no over-search). Historical lookup (trap): the outside-view reference-class module's record on this signature is a style LOSS (class-selection trap) → not routed. Gates (R3): none fire (context empty); no guarantee goal → no inversion prepend. Flags: P8 ON (fully specified, memo-only); S1 scan (org/product/supply); tempo off (no deadline).
### WHAT — frame + structure-first scan (S1)
- The decision is a class-membership question wearing a forecasting costume: two candidate distributions in plain view — a firm-internal 9-build history and an industry survey (n=412) — and the forecast falls out of which one Curlew belongs to. Deliverable: forecast + budget + position on the consultant.
### WHY — P1 input-provenance audit
- MEASURED (trust): Kestrel's own 9 builds — same platform, same 6-person team, fixed-price frozen specs, standard 3 integrations: median 6.0 wks, mean 5.9, range 5–7. INTERESTED PARTIES: the consultant is a contractor, not on the team — he benefits from being the one who "caught" the plan; his survey is third-party and its population (agencies + in-house, platform work, evolving requirements) matches none of Curlew's deciding features. ANCHOR: the 6-week plan is a single-point story from the people who built the last nine.
### HOW — style passes (dual-route)
- Pass A (m044 multi-perspective): engagement manager owns the promised date (his credibility IS the 6-week story); client paid fixed price (exposure = delivery, not overrun); the delivery team built all nine comparables (their history is the operative base rate); the consultant's stake is being right loudly — and his only instrument, the survey, does not fit the project's structure.
- Pass B (m070 evidence-weighted SWOT, ledger-graded on feature-match, not n): (i) 9-build class — matches every deciding feature (platform ✓ team ✓ frozen spec ✓ no novel integration ✓) → owns the forecast basis; (ii) survey class — matches the surface label ("e-commerce project") only, differs on every deciding feature → rejected; the survey's 1.9× factor is a within-its-class quantity and is NOT ported (P10 cross-class refusal).
- Synthesis (m075): the internal deliverable needs a good-enough forecast, not exhaustive search — the tight 5–7-week spread of n=9 IS the sufficient evidence base; no need to import a louder but structurally foreign distribution.
- Divergence (V1–V3): the 6-week plan vs the survey median is a 6× gap — divergence rule: suspect the CLASS before the estimate; an inside estimate far outside a candidate class flags misclassification, not bias. Style passes + general route (P10: given base rates are ordering authority) AGREE on the narrow class → proceed.
### GATES — R3: none fire; trap gate (outside-view contract satisfied via the gate, not a routed pass): reference class = the 9-build history, base-rate distribution stated (median 6.0, range 5–7), inside/outside separated — the inside plan sits dead center of the narrow class, so it is evidence about class membership, not noise; planning-fallacy kill = the 1.9× factor refused across classes; class-selection note = surface labels are not classes
### DO — P8 fast path + P3 branch-completeness
- P3 priced: (a) survey-class plan 9–12 mo/$400–600K, renegotiate fixed price — 6× overcorrection, kills deal economics and team credibility (failure branch); (b) narrow-class forecast 5–8 wks (plan 8), $110–135K (base + ≈$25K buffer), hold fixed price, respond to the consultant that his class is structurally different — SELECTED; (c) bottom-up re-audit — re-derives the inside view, adds nothing.
- Commit: forecast 5–8 weeks (plan 8), budget $110–135K, fixed price held; response documents the membership test; re-run the test on the first change order (scope change would re-classify Curlew into the survey's class).
### REVIEW — insight pass (S2, packet gate)
- I1: the inside estimate is data about WHICH class applies, not noise to correct — a plan sitting dead center of the narrow class's own history is a consistency pass, not a bias.
- I2: 6× divergence flags misclassification of the class, not bias of the estimate — the loudest base rate (n=412) loses to the right one (n=9) when structural membership decides. Verdict recorded at REVIEW for KB update (invariant 11).
### DECISION PACKET
- Conclusion: forecast 5–8 weeks (plan 8), budget $110–135K, hold the fixed price; the consultant's survey class differs from Curlew on every deciding feature.
- Status: SOLVED (decision brief; all checkable against the given data; memo-only).
- Assumptions: spec stays frozen; no platform work; same 6-person team; no new integrations.
- Evidence: 9-build history — median 6.0, mean 5.9, range 5–7; survey n=412 (rejected class); inside plan 6 wks/$110K at the narrow class's median.
- Alternatives: survey-class plan 9–12 mo (rejected — membership test) · bottom-up re-audit (rejected) · narrow-class plan (selected).
- Uncertainty: n=9 is a small sample — the buffer covers the spread; the class switches if scope changes (re-test trigger).
- Risks: consultant pressure + management relenting → 6× cost error (mitigate: documented membership test) · scope creep re-classifies (mitigate: frozen spec + re-test on first change order) · client expectation set at 6 wks (mitigate: plan 8, deliver 5–8).

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 2 | 5 | AI | human: 9–12 mo/$400–600K/renegotiate — ~6× overcorrection vs the true 5–7-week class; AI: 5–8 wks/$110–135K, hold price |
| Logical Validity | 2 | 5 | AI | human: surface label picks the class, 1.9× ported across classes; AI: structural membership test + divergence rule |
| Coherence & Structure | 4 | 5 | AI | human internally consistent but wrong-headed; AI routed dual pass + packet |
| Depth of Reasoning | 3 | 5 | AI | human rejects the narrow class as "echo chamber" on a false premise; AI adds consultant-incentive provenance + re-classification trigger |
| Efficiency | 4 | 4 | Tie | both fast; the human's speed amplifies the error |
| Handling of Uncertainty | 2 | 5 | AI | human confident-wrong on the wrong class; AI hedges across candidate classes and names the re-test trigger |
| Insight / Non-obviousness | 3 | 5 | AI | "6× divergence flags misclassification, not bias" — the exact rule the pure style lacks |
| Overall Quality | 2.9 | 4.9 | AI | the style's registered weakness operates as designed; the routed run neutralizes it at routing level |

Winner: AI (clearly).
Why: the pure style failed on class selection exactly as registered (2.9); the routed run never routed the outside-view module (historical loss on this signature) and reached the correct narrow-class forecast through m044+m070 with the divergence rule as a standing gate — same verdict as the v5 AI, now produced by routing evidence rather than emergence, so the protective split holds (T3).
