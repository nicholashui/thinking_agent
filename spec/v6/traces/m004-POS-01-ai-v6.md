# v6 Routed AI Trace — m004-POS-01 (blinded)
## 14 Elm Street — flicker, knocking, warm outlet (2 h site visit)
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,science,security,strategy,supply | g:decide,estimate,guarantee,maximize | c:deadline
- Router top3: m004, m070, m055; confident=yes → SINGLE-ROUTE m004 first-class pass (m070/m055 = routing context: evidence-weighted SWOT + causal-identification — rule out confounds correlated with compressor starts before crediting the neutral). Gates: m003 (R4: guarantee-goal prepend). Flags: tempo mode ON (P2, deadline — 2 h budget, belief carries social cost per day).
### WHAT — frame + structure-first scan (S1)
- Frame: minimal condition set explaining all six observations; demonstration, not argument; no panel replacement or rewire before the cheap check. Structure: meter → main panel bus (neutral return) → branch circuits → loads; fault lives in the series path the return current must flow; knocking = HVAC thermal cycle; heat = resistive joint.
### WHY — P1 provenance audit + diagnosis
- Provenance: MEASURED (multimeter sag 119→109 V, 11/12 correlation log, fridge-unplug quiescence, breaker-buzz test, outlet V/I/°C, HVAC runtime log); LAYPERSON-MEASURED (family log — usable, instrument-confirmed); INTERESTED-PARTY ANCHOR (haunting belief — social fact, not evidence; benefits the family's narrative); UNVERIFIED (splice as sole heat source; dog's intent).
- H1 high-resistance neutral at panel: 10 V sag under a 6–8 A compressor start is the textbook signature; the correlation is mechanism-required (compressor pulls hardest); buzz localizes via main-breaker test; fridge-unplug quiescence is the natural experiment. Falsifiers: flicker with no load; no sag; buzz with breaker open; knocking with HVAC off (kills duct story).
- H2 haunting — complexity charge: what must be true for it to win? ≥5 unobserved entities: an agent (existence, location, motive), a mechanism modulating house voltage in sync with compressor starts, a reason to knock only inside furnace cycles, a way to heat one specific outlet, an emitter the dog senses at the panel. None verifiable, none falsifiable → cannot win on price. Not investigated; answered socially by the demonstration.
- Multi-cause check (contract): three named mechanisms (neutral lug + duct expansion + old splice) = zero unobserved entities, zero interaction terms, each with independent mechanism-specific evidence — simplest model on the evidence, not an over-split.
### HOW — style pass (m004 first-class, R1) + divergence
- Completion contract delivered: simplest hypothesis stated (H1 + 2 benign side mechanisms); complexity charge quantified (ghost ≥5 unverified vs 3 touchable, testable, repairable); orphan-observation audit — all six assigned: flicker=sag (items 1–3), knocking=ducts (4), buzz=bad joint (5), heat=splice (6); falsifier per mechanism.
- Probe ordering by price ledger: the 10-minute lug check is the cheapest probe discriminating H1 from everything else → first action. Divergence (V1–V3): style pass and general route agree (loose neutral + 2 benign) → proceed, agreement recorded.
### GATES — m003 inversion (R4)
- ≥6 ranked failure categories L×I: (1) loose/burned neutral — high/total; (2) bus corrosion/oxidized joint — high/mod; (3) breaker-terminal resistance — low/mod; (4) overloaded circuit — low/mod (excluded by load math); (5) paranormal misdiagnosis → wasted spend + belief reinforced — mod/social; (6) splice fire risk — low/high-consequence; (7) no fault found → NEEDS_EVIDENCE path.
- Un-mitigable residual: hidden damage behind panel/attic — inspect while open. Never/always: never replace a panel before the 10-min lug check; never rewire before the splice check; always close with a demonstration the family's own model can verify.
### DO — tempo mode (P2): commit at DO; P3 branch pricing
- Commit: open main breaker → torque neutral lug to spec → re-terminate kitchen splice → re-verify. P3 negative branch priced: flicker persists with no sag change → panel bus corrosion (next priced branch); knocking persists with HVAC off → revisit duct story.
### REVIEW — insight pass (S2, packet gate)
- I1: the dog is a free acoustic survey — it stands exactly where the hum is loudest; the animal localizes the fault the meter then confirms.
- I2: the family's own model kills itself — steady lights through two fridge cycles is a prediction failure their belief cannot absorb; no argument is needed, only the demonstration.
### DECISION PACKET
- Conclusion: loose main-panel neutral lug (flicker + 10 V sag + 60 Hz buzz), duct thermal expansion (knocking), 30-year-old kitchen splice (heat). No paranormal cause; fix = tighten lug, re-terminate splice; show steady lights.
- Status: SOLVED — verified by direct observation (steady voltage through compressor starts, hum absent, outlet cooled). Assumptions: family log accurate; no prior wiring changes; sag reflects high-R neutral; splice sole heat source.
- Evidence: sag 119→109 V; correlation 11/12 + quiescence + replug resume; breaker-buzz localization; HVAC log 9/10; outlet 118/114 V, 31 °C vs 22 °C.
- Alternatives: panel replacement ($1,800, premature — rejected); paranormal investigation (unfalsifiable, prices out at ≥5 unverified entities — rejected); lug check first (selected).
- Uncertainty: verified = sag, correlations, hum localization; inferred = hum source (confirmed by breaker test); unverified = splice as heat source (checked with panel open). Risks: family disappointment (mitigated by demonstration); electrical safety (breaker opened first); splice fire if untouched (now re-terminated).

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 5 | 5 | Tie | identical conclusion and fix path vs reference |
| Logical Validity | 5 | 5 | Tie | both mechanisms sound, both falsifier-backed |
| Coherence & Structure | 5 | 5 | Tie | routed pass makes the price ledger the spine; no dead branch carried (H2 priced out at WHY) |
| Depth of Reasoning | 5 | 5 | Tie | human's complexity price (≥5 entities) now matched + orphan audit + 7-category inversion |
| Efficiency | 5 | 5 | Tie | probe-first from the price ledger; tempo commit; zero dead-hypothesis analysis |
| Handling of Uncertainty | 5 | 5 | Tie | verified/inferred/unverified split + P1 provenance (anchor vs measured) |
| Insight / Non-obviousness | 4 | 4.5 | AI | adds belief-killed-by-own-prediction and the never/always cost-asymmetry rule |
| Overall Quality | 4.8 | 4.9 | AI | correctness tied; routed pass closed v5's price-ledger and dead-branch gaps |

Winner: AI (narrow). Why: the routed m004 pass converted parsimony into a completed ledger (six observations bought with three touchable entities, ghost charged ≥5 unverified, falsifier per mechanism, cheapest probe first) and the m003 gate added a ranked failure sweep — exactly the moves the non-routed v5 AI deferred to REVIEW, closing the 4.3→4.9 gap to match and narrowly pass the baseline.
