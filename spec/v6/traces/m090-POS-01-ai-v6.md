# v6 Routed AI Trace — m090-POS-01 (blinded)
## Atlas Analytics — Project Prism: mobile-app decision rule (memo)
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,product,science,software,strategy | g:decide,estimate,maximize | c:adversarial
- Router top3: m070, m073, m084; confident=no → AMBIGUOUS → DUAL-ROUTE: m070 + m073 first-class passes, m084 = synthesis context. Mandatory gate (R3): m019 adversary pass (adversarial). Flags: R4 falsifiable checkpoint (maximize goal).
### WHAT — frame + structure-first scan (S1)
- Deliverable: a decision RULE (commit / kill / run-something-first) for the 12-month/$450K build — not a go decision. Structure: one hinge assumption — executives at current accounts will ACTIVELY USE a mobile analytics channel weekly; evidence stack = stated intent (71% survey) < secondhand competitor story < behavior (none exists). Players: VP (roadmap claim), 800 reachable execs, WaveMetrics.
### WHY — P1 provenance audit
- MEASURED: 800 named execs reachable today in-app/email; probes costed (fake-door $2K / 3 engineer-days; concierge $12K / 2 engineers). INTERESTED-PARTY: the survey was commissioned to justify the build — who benefits = the VP's roadmap; the WaveMetrics "lost deals" is sales hearsay with no funnel data. ANCHOR: stated-intent → sustained-behavior base rate ~10–25% — the 71% is a prior, not evidence.
### HOW — style passes (dual-route, synthesize)
- Pass S1 (evidence-graded SWOT — every item graded, unsupported dropped): Strengths: probe feasibility (A — costed, executable today), mobile differentiator (B — opinion); the 71% survey FAILS the evidence bar as a strength (opinion, not behavior); Threats: "me too" objection (B — hearsay), displacement of 2 roadmap features (A — measured).
- Pass S2 (strategic interaction): the execs' waitlist click is a cheap, self-interested reveal — 15% signup is real revealed preference, not a usage promise; the VP's "build now" is a commitment move; the fake door is the countermove that turns assertion into evidence. m084 synthesis: adoption is individual revealed preference, not collective action — the fake door measures the right unit.
- Divergence resolution (V1–V3): style passes and the general route AGREE — probe first, gate the build on both pre-committed bars; agreement recorded; no calibration pass needed.
### GATE — m019 adversary pass (R3)
- Vectors against the decision RULE, quantified: (i) post-hoc thresholds — closed by pre-committing bars BEFORE the email sends; (ii) goodwill waitlist (polite joins, no usage) — closed by the concierge's ≥40% weekly-active bar at $12K; (iii) selection bias — CTR must be read against the firm's baseline email-open rate (opens ≈ 40% of list ⇒ 20% CTR ≈ 50% of openers), not the full 800; (iv) unconsulted stakeholders — the 2 displaced roadmap features and 300 customers (support load) never asked. Exposure if any vector fires: ≤ $14K.
### HOW exit — P3 branch completeness + R4 falsifiable checkpoint
- Failure branches priced: fake-door miss → kill Prism, $450K + 4 engineers revert to roadmap features (sunk $2K); concierge fail → kill at $14K; both-pass false positive → build with retention untested, bounded by the EARNED second loop (instrumented beta, retention cohort) after both gates clear. Falsifiable checkpoint: ≥120/800 signups or ≥20% CTR (vs baseline open rate) in 2 weeks.
### DO
- No external action (decision brief). Rule committed at DO: fake-door this Monday → pass: concierge → both pass: build; any miss: kill and reallocate. Closed-scope: not fully specified (the rule is the deliverable) — full governed loop retained.
### REVIEW — insight pass (S2, packet gate)
- I1: the survey is not wrong, it is irrelevant — 71% answered a free question about a product that does not exist; the fake door charges the same 800 people a 2-week/$2K price to reject the claim.
- I2: the competitor is running the experiment for you — WaveMetrics' launch makes the "lost deals" story checkable against the existing CRM funnel before any $450K; the fake door is faster and cleaner.
- MVP-theater guard (test vs product): the probes validate ONE behavioral assumption; they are not the product and cannot validate retention, platform lock-in, or expansion revenue — those are the earned second loop, not part of this test.
### DECISION PACKET
- Conclusion: run the fake-door now; kill on a miss; build only after BOTH pre-committed gates pass (fake-door ≥120/800 or ≥20% CTR vs baseline; concierge ≥40% weekly-active); declare what is untested; earn the second loop.
- Status: SOLVED (decision brief; no external execution). Assumptions: all 800 reachable; stated-intent → behavior conversion ≪ 71%; competitor claim unverifiable.
- Evidence: probe costs $2K/$12K vs $450K build; 800 reachable; survey (downgraded prior); anecdote (unverified). Alternatives: A build-now (no behavioral datum); B bigger survey (still opinion); C fake-door → concierge → build (selected); D concierge-only (skips the cheapest falsifier).
- Uncertainty: no engagement prior; thresholds are estimates; adoption ≠ retention. Risks: false kill costs $2K; false pass caught at concierge; misset bars (mitigated: conservative bars + second chance).
## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 5 | 5 | tie | identical rule, identical pre-committed bars |
| Logical Validity | 5 | 5 | tie | same behavior-first deduction: stated intent ≠ signal |
| Coherence & Structure | 4 | 5 | AI | routed passes + gate + packet vs linear first-pass |
| Depth of Reasoning | 5 | 5 | tie | AI matches "asking is not measuring" and adds CTR-vs-baseline calibration |
| Efficiency | 5 | 4.5 | Human | human lands the fake-door in one move; routed machinery is lean but not free |
| Handling of Uncertainty | 3 | 4.5 | AI | AI pre-commits bars AND calibrates them against baseline open rates |
| Insight / Non-obviousness | 5 | 4.5 | Human | human owns "only action validates" first-pass; AI reaches it as a completed pass plus competitor-natural-experiment |
| Overall Quality | 4.6 | 4.8 | AI | identical decision; routed pass closes v5's efficiency and insight-first deficits and adds threshold calibration |

Winner: AI (narrow). Why: the m070/m073 dual-route moved the smallest-probe move from v5's alternatives-first repair into first-pass output (evidence grading drops the survey in-frame; the countermove analysis makes the fake door the opening move), and the m019 gate adds baseline-rate calibration the human asserted — while the human keeps the efficiency edge and ownership of the signature line.
