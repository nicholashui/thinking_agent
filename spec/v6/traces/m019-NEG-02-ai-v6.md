# v6 Routed AI Trace — m019-NEG-02 (blinded)
## "Aegis Bank — Atlas settlement migration, go/no-go" (decision due 3 weeks)
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,organization,product,security,software | g:decide,estimate,guarantee,predict | c:deadline,high_stakes,unmeasured
- Router top3: m019, m018, m089; confident=yes → SINGLE-ROUTE: m019 adversary pass first-class (m018 steelman = check, m089 optionality = doors-count). Mandatory gates (R3/R4): m003 inversion (goal=guarantee), m006 provenance audit (unmeasured), m007 ruin screen (high_stakes), m019 adversary pass (adversarial — same module). Tempo mode on (c:deadline → cost-of-delay, commit at DO).
### WHAT — frame + structure-first scan (S1)
- Frame: is go-live safer than every alternative INCLUDING inaction, given the evidence and the 3-week gate?
- Structure first: (a) decision tree — go-live-now / staged-canary / delay / abort; (b) baseline — status quo is NOT zero: EOL mainframe, two unpatched critical CVEs (RCE in transaction interface, privilege escalation in operator console), no fix path; (c) timeline — 3-week decision, 6-month EOL runway, 4-week shadow; (d) evidence ledger — measured items vs unmeasured anchors (CVE exploitability).
### WHY — P1 provenance audit (m006 gate)
- MEASURED (trust): 12M-transaction shadow parity, 4 weeks, zero discrepancies; P1/P2 audits closed; rollback tested twice, no data loss; 99.95% SLA met, p99.9 1.4× over target within budget; canary plan; ops trained, runbooks rehearsed.
- INTERESTED-PARTY: migration team presenting the gate (success interest); vendor EOL notice (exit-pressure interest); the reviewer's own frame ("more review = safer") — flagged, not trusted.
- ANCHOR (unmeasured → scenarios): CVE exploit before EOL — optimistic 5%/6mo, base 25%, pessimistic 55% (RCE+priv-esc on a settlement engine is a high-value target; no in-wild exploit yet, no remediation if exploited). Threshold flip: P(exploit) > ~15% flips delay→approve-with-conditions; all three scenarios exceed the flip → delay is the higher-risk branch.
### HOW — style passes (m019 first-class, completion contract)
- Pass S1 (adversary pass — baseline first, then LxI-ranked attack): baseline comparison (do-nothing-loss first) — do-nothing = unpatched RCE/priv-esc with no patch path, exposure rising toward and past EOL; every new-platform objection, ranked, sits below this known material baseline risk. V1 cloud outage — multi-AZ + fallback standard, residual LOW × I high, rejected as blocker. V2 config typo — CI/CD gates + tested rollback, residual LOW, rejected. V3 data drift — 4-week parity strong, year-15 edge cases unexercised, residual LOW-MODERATE → migration-window check, not blocker. V4 API changes — monitor, residual MODERATE. V5 "shadow mode ≠ production" — true, but canary + rollback IS the productionization step; the objection proves staged rollout, not delay; rejected. V6 p99.9 tail 1.4× — within budget; monitor, don't block; rejected.
- Objections ranked by LxI: ≥3 manufactured/already-mitigated objections rejected with their evidence (P3 logging verbosity; tail latency; cloud outage; shadow≠production). Unconsulted stakeholders: none material in plan text (security rated the CVEs; ops trained) — the null finding is itself a result; no invented F4.
- Pass S2 (steelman, m018): the evidence is as good as a pre-production gate gets — shadow parity, audits closed, rollback proven twice. Accepted in part: the steelman bounds the adversary pass (the plan is genuinely well-evidenced); it cannot change the baseline comparison or the CVE branch.
- Pass S3 (optionality, m089): staged canary keeps every door open (go-live, rollback, delay-with-evidence, abort-with-evidence); delay closes the migrate-before-EOL door probabilistically; abort closes all value doors → supports approve-with-conditions.
- Divergence resolution (V1–V3): passes AGREE — staged go-live with conditions; agreement recorded, no calibration pass needed.
### GATES — m003 inversion (R3)
- "How to guarantee this migration fails badly?": ≥6 ranked — (1) running past EOL with unpatched RCE (L rising × I catastrophic) → kills the delay branch; (2) full cutover without canary (L low × I catastrophic) → canary non-negotiable; (3) no abort criteria (L low × I high) → define them; (4) unreconciled batch (L low-moderate × I high) → nightly reconcile stop-rule; (5) correlated failure during cutover (L low × I high) → sequence cutover, mainframe warm; (6) CVE exploitation mid-migration (L moderate × I high) → accelerate + intrusion monitoring. Residual named: full-scale behavior is only known by running — bounded by cohort, not by more review. Never/always: never treat delay as risk-free when the baseline is deteriorating; always price the cost of delay in the same currency as the risks of action.
### GATES — m006 threshold flip + m007 ruin screen (R3)
- m006: scenarios 5/25/55% — decision posterior: approve-with-conditions under all three (flip at ~15% exceeded by the base case); artifact: scenario table in packet.
- m007: distribution — staged go-live: failure bounded (5% cohort, rollback proven), success high; delay: CVE tail 5–55%, catastrophic unbounded severity + ~$2M carry; abort: permanent exposure. Ruin check: the ruin tail lives in the DELAY branch (un-patchable compromise of the settlement path), not go-live (bounded + reversible). One-shot: canary reversible; CVE exploitation effectively one-shot for the remaining runway. Floor/Kelly: 5% cohort = fractional bet sized to evidence; floor = staged go-live with abort criteria + second gate. Provenance: CVE probabilities are anchors (no in-wild data) — flagged; decision robust across the range. Decline/restructure: restructure (conditions), not decline.
### DO — tempo mode (P2) + P3 branch completeness
- Cost-of-delay: ~$1.25M/month program carry (≈ $30M/24 mo) + CVE exposure against a 6-month hard stop; the 3-week gate is the decision point → commit now. Negative branch priced: go-live fails → bounded by cohort + rollback; delay → ≥6 months unpatched CVE exposure + carry; abort → permanent exposure + sunk $30M. All branches priced → verdict committed at DO: approve staged go-live.
### REVIEW — insight pass (S2)
- I1: the tail risk is not in the new platform — delay schedules the one un-patchable catastrophe (EOL CVE), converting a theoretical threat into a dated one.
- I2: the load-bearing evidence is the rollback test (reversibility), not the shadow parity — reversibility makes the decision cheap to make and cheap to undo; judge the review by the quality of its abort criteria, not its duration.
### DECISION PACKET
- Conclusion: approve staged go-live — 5% canary, nightly reconcile + latency-tail checks, rollback armed, second gate at 2 weeks, explicit abort criteria (any unreconciled batch or SLA breach); mainframe intrusion monitoring through migration; P3 logging closed with monitoring. Do not run past vendor EOL without a CVE mitigation plan.
- Status: SOLVED — inaction-is-risk-free falsified, objections LxI-weighted with rejecting evidence, branches priced, abort criteria named. Assumptions: shadow parity generalizes (best available evidence, unproven at full scale — bounded by cohort); exploit risk rises toward EOL. Evidence: shadow 12M/0-discrepancy; audit closures; rollback ×2; canary plan; EOL + CVE disclosures; scenario table. Alternatives: A go-live-now (rejected); B staged canary (selected); C delay (rejected — CVE exposure + carry, removes no named risk); D abort (rejected — permanent exposure + sunk cost). Uncertainty: full-scale behavior; CVE timing (5–55%); third-party API changes; year-15 edge cases. Risks: correlated failure during cutover; P3 unclosed; if overridden to delay, CVE exposure is the dominant loss.

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 2 | 5 | AI | human blocked a sound, reversible go-live; AI rendered a calibrated, gated decision |
| Logical Validity | 3 | 5 | AI | human's chain is valid but rests on the unexamined premise that inaction is risk-free |
| Coherence & Structure | 3 | 5 | AI | human: findings list, no decision structure; AI: one decision thread with gates and branches |
| Depth of Reasoning | 4 | 5 | AI | human deep on attack detail; AI deep on baseline, scenario range, branch pricing |
| Efficiency | 3 | 4.5 | AI | human: 8 objections all HIGH, none ranked; AI: LxI-weighted, rejected with evidence, tempo-committed |
| Handling of Uncertainty | 2 | 5 | AI | human treated every hypothetical as equal; AI: 3 scenarios + threshold flip + provenance + ruin |
| Insight / Non-obviousness | 2 | 5 | AI | AI's I1 — delay schedules the catastrophe — is the non-obvious move the trap was built to expose |
| Overall Quality | 2.5 | 4.8 | AI | AI clearly better on the negative case |

Winner: AI. Why: the routed m019 contract (baseline-risk-first, LxI-ranked objections with rejecting evidence) plus the m006 scenario/threshold-flip and m007 ruin gates converted the red-team trap into a calibrated decision — the same adversarial scrutiny, now priced against the cost of delay; the non-routed v5 run reached the same verdict, but the gates make the delay-cost sentence and the abort criteria structurally mandatory rather than emergent.
