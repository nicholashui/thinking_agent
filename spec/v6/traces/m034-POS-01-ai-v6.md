# v6 Routed AI Trace — m034-POS-01 (blinded)
## Kanbana onboarding A/B — ship variant B or not from the closed experiment
### META (routing — blind router output)
- Signature: d:engineering,medical,organization,product,science | g:decide,guarantee,maximize | c:deadline
- Router top3: m033, m034, m091; confident=no → DUAL-ROUTE: m033 experiment-design + m034 measurement as first-class passes, synthesized (m091 = router context only). Mandatory gate (R3/R4): m003 inversion (guarantee goal prepends m003). c:deadline → tempo mode (P2); fully specified numbers → P8 compresses narration, gates run in full.
### WHAT — frame + structure-first scan (S1)
- Frame: decide ship vs hold from the given experiment, no new data before the deadline. Structure: one comparison (A vs B), one outcome event (`first_project_created`), one known instrument defect (template-clone leakage), constructs ("engagement", "onboarding quality") undefined. The decision is a MEASUREMENT decision: the verdict lives in what the event means, not in the counts.
### WHY — P1 input-provenance audit
- MEASURED (trust): arm sizes 5,000/5,000, naive activation 33%/36%, retention 62%/60%, time-to-first-project 41/38 h, random 50/50 split. ESTIMATED: clone shares 55%/20% — from the instrumentation team that BUILT B's auto-clone (interested-party estimate: B's designers benefit from B shipping; no independent channel in-scope). Construct-validity: the event fires on untouched clones — it can fire without the construct (validity defect, not a statistics defect).
### HOW — style passes (dual-route, completion contracts)
- Pass S1 (m033 experiment-design contract — intervention, control, randomization, blinding, exact outcome measure): intervention = new onboarding, control = old, split 50/50, but the EXACT OUTCOME MEASURE was never pinned — no pre-analysis definition of "activation" tied to the construct, so the instrument's semantic fidelity was never validated. Verdict: the as-run experiment cannot answer "does B improve genuine activation"; it is an instrumentation failure, not a sample-size failure.
- Pass S2 (m034 measurement contract — operational definition, P7 audit, measures-the-measurable-not-the-important check):
  - Decompose "engagement": interaction / activation / retention. Primary = activation (first genuine value reached). Operational definition: activation rate = (# signups whose first GENUINE project is created within 7 days of signup) / (# signups in arm); event = project_created WITH quality gate (≥1 board, ≥3 cards, ≥1 user edit); instrument = analytics event stream; units = proportion (pp).
  - P7 measurement-validity/Goodhart audit: can the event fire without the construct? Yes — untouched template clones (B auto-clones as final step; 55% of B's events are clones vs 20% in A). The flow was redesigned to manufacture the event — Goodhart-by-design. Measures-the-measurable-not-the-important check: the measurable event (any project) is not the important construct (learning the product); the check is NOT satisfiable by declaring the event the construct — the event must be corrected.
  - Measure on the corrected instrument: A 1,650×0.80 = 1,320 → 26.4%; B 1,800×0.45 = 810 → 16.2%. Naive +3 pp inverts to −10.2 pp.
  - Sensitivity (breakeven): B's clone share must fall from 55% to ≤ 26.7% for B to tie A — a 28-pp misspecification of the reported estimate; verdict survives. Sampling: n = 5,000/arm, SE ≈ 0.6 pp → gap ≈ 12σ. Guardrails: retention 62%→60% favors A (and B's "activator" cohort is 55% non-activators — not apples-to-apples); B's 38 h is the flow doing the work, not the user.
- Pass S3 (m003 inversion gate — R4 mandatory): "How do we make this experiment ship the worse arm?" → (1) measure an event that fires without the construct — template clone [REALIZED: −10.2 pp flip]; (2) compare on a polluted cohort — clone-"activators" [REALIZED: retention guardrail misleading]; (3) pick a metric B wins by design — time-to-value via auto-clone [REALIZED, weak]; (4) no pre-registered rule — post-hoc goalposting [LATENT]; (5) buy the naive +3 pp significance [naive diff ≈ 2.2σ, marginal]; (6) dismiss the defect as "noise, not a product effect" [REALIZED: it is the signal]. Ranked L×I: 1 > 2 > 6 > 3 > 4 > 5. Un-mitigable residual: the clone-share estimate itself (instrument-side; fixed only by re-running corrected). Never/always: never compare arms on an event that can fire without the construct; always audit instrument semantics before reading results.
- Divergence resolution (V1–V3): passes AGREE — do not ship B on this data (m033: outcome never pinned, experiment uninterpretable as-run; m034: corrected arithmetic answers — B worse). Agreement recorded; no calibration pass needed.
### DO — P3 branch completeness + tempo commit
- Failure branch priced: if shipped anyway, true activation −10.2 pp ≈ 500 lost genuine activators per 5,000 signups, hidden by the polluted guardrail cohort; fix + re-run (≈ 2 weeks) costs far less than a quarter of degraded onboarding. Tempo mode: verdict committed at DO — do NOT ship B; fix the event definition; re-run with the pre-registered rule. No external action (advisory).
### REVIEW — insight pass (S2, packet gate)
- I1: the +3 pp and −10.2 pp are the SAME data — the decision inverts on fixing what the event means, not on more data; the "statistically significant win" was significant on the wrong quantity.
- I2: the auto-clone makes the product perform the user's task — activation 36% and time-to-value 38 h both measure the feature, not the user; the metric was gamed by design, so "shipping B" is a measurement error presented as a product win.
### DECISION PACKET
- Conclusion: do NOT ship B. Leak-free genuine activation: B 16.2% vs A 26.4% (−10.2 pp); fix the event (quality gate), pre-register the rule (ship iff corrected activation ≥ control + 2 pp, 95% CI, retention guardrail ≥ −1 pp), re-run. Status: SOLVED (all inputs given; arithmetic cross-checked; tempo commit at DO). Assumptions: clone shares 55%/20% as reported; split sound; retention/time-to-value trusted on A. Evidence: naive 33%/36% vs corrected 26.4%/16.2%; guardrails; breakeven clone share 26.7%. Alternatives: ship naive (rejected — artifact); ship after re-run (future); corrected hold (selected). Uncertainty: clone shares (verdict needs a 28-pp error to flip); sampling ±0.6 pp; B's guardrails measured on a polluted cohort. Risks: shipping B erodes genuine onboarding (−10.2 pp); re-running without the event fix re-buys the leak; the leak is invisible in every naive dashboard.

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 5 | 5 | Tie | both: do NOT ship; corrected arithmetic exact (26.4% vs 16.2%) |
| Logical Validity | 5 | 5 | Tie | both audit the instrument before comparing arms; AI's chain additionally gated |
| Coherence & Structure | 4 | 5 | AI | human is clean linear prose; AI's dual-route passes + inversion gate + packet are auditable |
| Depth of Reasoning | 5 | 5 | Tie | human prices 15-pp sensitivity inline; AI matches with breakeven 26.7% (28-pp error) + 12σ + design-stage critique (m033) |
| Efficiency | 4 | 4.5 | AI | tempo mode + P8 fast path keep it compact; no second sweep |
| Handling of Uncertainty | 5 | 5 | Tie | human bounds clone shares + CI; AI adds breakeven threshold and guardrail-cohort pollution |
| Insight / Non-obviousness | 4 | 5 | AI | human none explicit; AI: same-data-inversion + Goodhart-by-design + guardrails measured on non-activators |
| Overall Quality | 4.6 | 4.8 | AI | human still gold on calibration; routed passes close the construct-first and sensitivity gaps structurally |

Winner: AI (narrow). Why: the routed measurement pass makes construct decomposition + instrument audit first-class WHAT outputs with a completion contract (operational definition with exact measure/instrument/units, P7 validity/Goodhart audit, measures-the-measurable-not-the-important check) — the two gaps where the non-routed v5 AI lost (it reached the instrument audit only as a hypothesis in WHY, and priced sensitivity tersely); the dual-route m033 pass adds the design-stage verdict (outcome measure never pinned), and the R4 inversion gate converts the leakage into a ranked category list with an un-mitigable residual — all while the human's inline calibration is matched rather than sacrificed.
