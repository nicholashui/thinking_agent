# v6 Routed AI Trace — m028-POS-01 (blinded)
## "Meridian Tower" — elevator-complaint capital decision ($4.2M/$7.8M quotes vs experience fix)
### META (routing — blind router output)
- Signature: d:engineering,finance,medical,organization,product,science,software,strategy | g:decide,diagnose,estimate,guarantee | c:high_stakes
- Router top3: m028, m084, m018; confident=yes → single route, m028 first-class pass (m084/m018 = synthesis context). Mandatory gates: m003 inversion (R4, g:guarantee), m007 ruin screen (R3, c:high_stakes). No c:deadline → no tempo mode; not fully specified (perception hypothesis open, gate data missing) → no closed-scope fast path.
### WHAT — frame + structure-first scan (S1)
- Structure: one decision (invest) × two given alternatives (modernize $4.2M / two new shafts $7.8M) + implicit decline; success metric = lease-renewal complaints; the brief bakes "wait time" into the goal. Frame accepted tentatively, flagged for the style pass: "the fix must reduce actual wait time."
### WHY — P1 input-provenance audit
- MEASURED (contract): 2-week sensor study (38s peak vs ~45s benchmark — not a failure), complaint timestamps (off-peak, <15s waits), quotes, survey wording ("elevator experience" — metric-vs-goal mismatch: the metric measures perception, not duration). ANCHOR: none unmeasured in the decision rule. INTERESTED-PARTY: engineering vendor benefits from hardware framing; tenants in a soft market benefit from complaint attention.
- ARCHIVED-NATURAL-EXPERIMENT: 9-year-old facilities memo — full-length mirrors → complaints fell sharply 14 months; renovation removed them; complaints returned. Same building, same lobby — a within-building control the $4.2M plan lacks.
- Audit conclusion: no measured input supports "complaints ⇒ actual wait too long" (38s ≈ 45s benchmark); the strongest documented lever in this building is experience. H1 wait-duration drives complaints — falsified; H2 perception/experience drives complaints — supported (3x survey/sensor gap, dead-zone lobby); H3 mirror effect replicates — in-building precedent, testable.
### HOW — style passes (single route)
- Pass S1 (m028 assumption-break — name assumptions, generate from each break): assumptions (a) problem = elevator speed; (b) complaints track actual wait duration; (c) fix must be hardware/capital; (d) decision set = {modernize, shafts}. Breaks: (a)+(b) → the complaint is the experience of waiting → perception interventions (mirrors — memo-backed, arrival displays, seating/greenery, wi-fi boost); (c) → stair fast-track floors 2–4 + lobby package <$200K; (d) → decline-and-experiment: run the package 8 weeks with a dual metric (complaints + sensors), hardware quotes held as staged fallback.
- Hard-constraint screen AFTER ideation (P5): physical (elevators must still move people — package respects capacity, no safety exposure), capital (<$200K), deadline (8-week gate fits). SOFT: "wait must be shortened", "lobby must be blank", "mirrors are unserious". Screen: PASS — no generated option breaks a hard constraint.
- Pass S2 (m084 context): the lobby is a shared good the building owns — the memo proves management can cheaply supply it; the leasing pitch becomes "an experience that isn't a wait." Pass S3 (m018 context, steelman): hardware's strongest form — if waits were the driver, $4.2M is durable; sensors say they aren't — the steelman fails on the given data.
- Divergence resolution (V1–V2): general route agrees with the style pass (experience fix primary, hardware gated fallback); agreement recorded.
### GATES — m003 inversion (R4) · m007 ruin screen (R3)
- m003 (>=6 failure categories, ranked LxI): (1) package fails to move complaints (H2 wrong) L-med I-high; (2) "mirrors are unserious" optics stall the committee L-med I-med; (3) market noise confounds the 8-week measure L-med I-med; (4) genuine hardware need delayed a leasing cycle L-low I-high (mitigated: gate is 8 weeks, quotes held); (5) gate threshold miscalibrated L-low I-med (dual metric); (6) survey rewording confounds the gate L-med I-high. Un-mitigable residual: whether the perception gap persists — only the experiment answers. Never/always: never $4.2M modernization on an unmeasured perception hypothesis; always test the $150K experience hypothesis with a dated go/no-go first.
- m007 (distribution, ruin, floor, provenance): package worst case = $150K + 8 weeks, quotes intact (no ruin); modernization worst case = $4.2M + disruption + complaints unchanged if perception-driven; no ruin in either, downside asymmetry favors the experiment; spend floor $150K; probabilities are manager expectations, not measured — the gate is the calibration device.
### DO — P3 branch completeness
- Branches priced: success (complaints −>=25% → cancel hardware, reposition pitch), failure (→ stage modernization, quotes held), mixed (waits still high, perception better → modernization justified on sensor data). Advisory; verdict committed: experience package + 8-week dual-metric gate.
### REVIEW — insight pass (S2, packet gate)
- I1: the renewal survey asks about "elevator experience" — complaints are already the answer to a perception question, so the marginal lever is experience, not speed; the sensor study proves the hardware premise false before any money moves.
- I2: the memo is a within-building natural experiment with a control (renovation = removal) that the $4.2M modernization lacks — the cheapest falsifier in the packet already ran once, 9 years ago.
### DECISION PACKET
- Conclusion: mirrors + arrival displays + seating/greenery + wi-fi boost + stair fast-track ≈ $150K, ~3 weeks, zero disruption; 8-week gate (complaints + sensors vs baseline); −>=25% → cancel hardware study, reposition pitch; else stage modernization with quotes held. Cost comparison: $150K experiment vs $4.2M/$7.8M capital.
- Status: NEEDS_EXPERIMENT (probe = 8-week dual-metric gate). Assumptions: complaints track perception more than duration (H2); mirror effect replicates (H3); 8 weeks representative. Evidence: sensor study (38s vs 1:55), off-peak timestamps, survey wording, mirror memo, quotes. Alternatives: modernization (rejected: premise falsified), shafts (rejected: $7.8M/18 months), package+gate (selected), do nothing (rejected). Uncertainty: H2/H3 strength (gate answers), committee optics, market drift. Risks: gate fails → 2 months + deferred hardware (bounded $150K); gate miscalibrated (dual metric); genuine hardware need delayed (quotes held).

## Comparison
| Dimension | Human | AI | Winner | Notes |
|---|---|---|---|---|
| Goal Achievement | 5 | 5 | Tie | both break the frame → $150K experiment + gate |
| Logical Validity | 4 | 5 | AI | human's causal leap locally evidenced; AI's P1 audit + constraint screen make the same move checkable |
| Coherence & Structure | 3 | 5 | AI | human associative; AI packet staged with gates and branch pricing |
| Depth of Reasoning | 5 | 5 | Tie | human reads all buried evidence into one story; AI adds metric-vs-goal, inversion ranking, ruin comparison |
| Efficiency | 5 | 4.5 | Human | pure style compresses to one pass; routed run pays m003/m007 gates (value-adding, not churn) |
| Handling of Uncertainty | 4 | 5 | AI | human's gate as answer; AI declares likelihood provenance, prices worst cases, dual metric |
| Insight / Non-obviousness | 5 | 5 | Tie | human's "regression we caused"; AI's metric-vs-goal + memo-as-natural-experiment |
| Overall Quality | 4.7 | 4.8 | AI | routed m028 pass delivers the frame-break the v5 run missed; the gates hold it safe |

Winner: AI (narrow). Why: the routed m028 pass closes the v5 loss — the frame ("fix = shorten real wait") is now named and broken as a first-class pass, and P1 + m003/m007 convert the human's brilliant reframe into a recommendation with a priced failure branch, ruin comparison, and dual-metric gate at a small efficiency cost.
