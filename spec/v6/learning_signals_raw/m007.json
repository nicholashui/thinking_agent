[
  {
    "test_case_id": "m007-POS-01",
    "human_model": "Expected Value Thinking",
    "winner": "ai",
    "key_gap": "vs baseline: the pure-style baseline solved the fully specified problem exactly in one pass (B $405k vs A $365k, both sensitivities: subcontract m* = $1.60, P(High) h* = 3/44) — nothing to correct in the arithmetic. The v5 AI tied with an efficiency gap (extra verification passes on checkable arithmetic). The routed run (m019/m023/m070 dual-route + m003 inversion gate, closed-scope fast path) kept the same one-pass arithmetic and added what the pure style cannot produce: quantified flip thresholds with the exposure per vector (h* = 6.8% = a 73% downward error needed; missing-demand-tail priced decision-neutral at 300epsilon for both machines), the shape-robustness structure (EV(B) = 3.E[D] - 120 is linear in mean demand while EV(A) = 345 + 80h depends only on P(High) - the whole decision collapses to two statistics), and the floor trade-off (A's worst state 250 > B's 180; risk-neutrality, not the probabilities, is the load-bearing assumption).",
    "learning_signal": "On fully specified risk-neutral EV problems the pure style's arithmetic is exact and unbeatable on numbers; the v6 win came from (a) the closed-scope fast path closing the efficiency gap, and (b) the routed adversary/opportunity-cost/inversion passes converting the sensitivity section into provable robustness claims - flip thresholds with exposure magnitudes, distribution-shape immunity, and the identification of which assumption actually carries the decision (risk-neutrality, not the stated probabilities).",
    "suggested_improvement": "Keep the closed-scope fast path for fully specified EV signatures (P8); make 'decision collapses to which summary statistics' a standing move in EV passes (linearity of the EV in the free parameter exposes robustness for free); require a floor-per-alternative line plus a flip-threshold statement (which parameter, what error magnitude) in the packet whenever an adversary or opportunity-cost pass runs on a numeric decision."
  },
  {
    "test_case_id": "m007-NEG-01",
    "human_model": "Expected Value Thinking",
    "winner": "ai",
    "key_gap": "vs baseline: the pure-style baseline executed the documented trap - internally correct stake-only EV (0.55 x 2.0 = $1.1M > $1.0M, 'take the bet', 'wide margin of safety' on p) that stops at the mean: no $0.8M payment floor, no absorbing ruin state destroying the $5.0M continuation stream (true comparison $5.2M vs 0.55 x 6.2 = $3.41M, decision inverts), no one-shot/LLN check. The v5 AI already declined (win) by thoroughness; the v6 routed run (dual-route m001/m018/m019 + mandatory m007 ruin gate) makes the catch structural, not lucky: full outcome distribution incl. continuation destroyed at ruin, log-utility -infinity at 45% ruin mass, Kelly 10% vs 100% demanded (10x over-bet), floor unreachable in the loss state, provenance (p* = 26/31 ~ 0.84 vs 0.55 claimed, C* = 46/45 ~ $1.02M vs $5.0M - an 80% haircut), decline/restructure alternative, EV-positivity-is-not-acceptability framing.",
    "learning_signal": "When a bet is one-shot, all-stake, and sits above a hard floor with an absorbing ruin state, the pure style's failure (stop at the mean) is closed by a mandatory ruin-gate contract that must include: the continuation value destroyed at ruin in the outcome table (the decision flips once it is priced), the one-shot/LLN check, floor and Kelly computations, probability provenance with breakevens, and an explicit decline/restructure alternative - plus the framing rule that EV-positivity on a stake-only account is not acceptability. Divergence resolution among dual-route passes (first principles, steelman, adversary all price the continuation state and agree) is what makes the verdict robust rather than lucky.",
    "suggested_improvement": "Keep the m007 ruin screen as the standing R3 gate for any high_stakes/one_shot signature and extend it to decisions with a hard floor or absorbing state even without one_shot; require a provenance line for every probability that dominates an EV table (interested-party claim vs measurement; required-value breakeven); add the over-Kelly computation (fraction demanded vs Kelly fraction) as a standing packet line for all-in stake offers."
  },
  {
    "test_case_id": "m007-POS-02",
    "human_model": "Expected Value Thinking",
    "winner": "ai",
    "key_gap": "vs baseline: the pure-style baseline solved the fully specified problem exactly in one pass (EV B 0.9 vs C 0.8 vs A 0; posteriors 8/11, 1/7; EV(launch|Low) = 0 exactly; breakeven test price $0) — nothing to correct in the arithmetic; the v5 AI already tied with an efficiency gap. The v6 routed run (m023/m050 dual-route + m070 SWOT + m003 inversion gate, closed-scope fast path) kept the same one-pass arithmetic and added what the pure style cannot produce: flip thresholds with error magnitudes (p* = 1/7, a 64% downward error on the prior; a* = 0.80 breakeven test accuracy — the test pays only above 80%), the parameter-cancellation structure (the zero-information-value result holds exactly at the stated (a = 0.80, p = 0.40) pair: P(High|Low signal) = 1/7 makes EV(launch|Low) = 0 exactly, so the signal policy replicates always-launch), and the opportunity-cost reframing (the test adds zero EV, so its opportunity cost is its full price — buying it is a pure transfer).",
    "learning_signal": "On fully specified risk-neutral EV problems the pure style's arithmetic is exact and unbeatable on numbers; the v6 win comes from (a) the closed-scope fast path closing the efficiency gap, and (b) the routed opportunity-cost/inversion/SWOT passes converting the sensitivity section into provable robustness — flip thresholds with exposure magnitudes, the parameter pair where the non-obvious result holds, and identification of which input actually carries the decision (the prior, not the test). The zero-info result is a coincidence of the stated parameters, and stating why it holds is the non-obvious increment.",
    "suggested_improvement": "Keep the closed-scope fast path for fully specified EV signatures (P8); make 'decision collapses to which statistics' a standing move (EV(B) = 3.5p − 0.5 depends only on the prior; test value depends only on accuracy); require a flip-threshold line (parameter, error magnitude, breakeven) plus a 'where does the non-obvious result hold' statement in the packet whenever an opportunity-cost/adversary/inversion pass runs on a numeric decision."
  },
  {
    "test_case_id": "m007-NEG-02",
    "human_model": "Expected Value Thinking",
    "winner": "ai",
    "key_gap": "vs baseline: the pure-style baseline executed the documented trap — internally correct EV (0.90 x 200 = $180M > $100M, 'take the bet', 'wide margin of safety' on p) that stops at the mean: no $100M floor screen (every s > 0 has a 10% breach probability; only s = 0 admissible), no ruin/log-utility check (−∞), no one-shot/LLN check, no provenance (p = 0.90 is the counterparty's self-serving claim). The v5 AI already declined (win) by thoroughness; the v6 routed run (top-1 m024 regret + m011/m018 context + mandatory m007 ruin gate + m003 inversion gate) makes the catch structural: constrained minimax-regret independently selects keep (expected regret mirrors the EV trap — 10M vs 90M favors the bet — but the constrained catastrophic branch is unbounded), the steelman survives only in its escrow version (restructuring), and acceptability requires p = 1.0 (a guarantee), not p > 0.5.",
    "learning_signal": "For one-shot all-in offers above a hard floor with an absorbing state, the decision is not about probabilities at all: acceptability requires a guarantee (p = 1.0), not EV-positivity (p > 0.5) — the mean, the mode, and the floor tell three different stories. The 'stop at the average' failure is frame-invariant (it reappears in expected-regret) and the constraint resolves it identically in every frame; the steelman's strongest rescue is escrow, and the counterparty's refusal to post it is evidence. Divergence agreement across regret/ruin/systems passes is what makes the verdict robust rather than lucky.",
    "suggested_improvement": "Keep the m007 ruin screen as the standing R3 gate for high_stakes/one_shot signatures and extend it to any hard-floor or absorbing-state decision even without one_shot; require a provenance line for every probability that dominates an EV table (interested-party claim vs measurement; acceptability threshold vs EV breakeven — they are different numbers); add the over-Kelly computation (fraction demanded vs Kelly) and a constrained-minimax-regret line to the standing packet for all-in stake offers."
  }
]
