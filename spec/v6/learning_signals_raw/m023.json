[
  {
    "test_case_id": "m023-POS-01",
    "human_model": "Opportunity Cost Thinking",
    "winner": "ai",
    "key_gap": "vs baseline: the human baseline made the $1.5M forgone-alternative comparison the constitutive frame in-frame (sunk $370K excluded, verdict B with Elena, all rubric items) while the non-routed v5 AI computed the same numbers but buried the comparator — the opportunity-cost spine was reached only at REVIEW, status APPROXIMATED, and it lost 5.0/4.0 on the style's home turf. The routed run installs m023 as a first-class pass with an explicit completion contract (best-forgone-alternative valued explicitly, sunk-cost stripped, resource-discipline check), so the comparator is the spine of HOW, not a REVIEW discovery; m044 flags the board chair as interested party (the sunk-cost pitch is motivated, not just fallacious); m003 and m019 gates add >=6 ranked failure categories and enumerated exploit vectors with quantified exposure in-frame; P1/P3 run before selection; verdict flips to an AI narrow edge on breadth.",
    "learning_signal": "On decide/maximize signatures with sunk spend and a scarce-resource fork, computing the opportunity cost is necessary but not sufficient: the frame that defines the decision ('what does this choice forgo, and does the forgone beat it?') must LEAD the pass, or the math is right and the reasoning still loses on the style's home turf; P1 interested-party provenance upgrades 'the chair's fallacy' from a refutation target into the first audit (who benefits is a first-class input property), and the $1.5M gap is a decision line, not a supporting calculation.",
    "suggested_improvement": "Keep m023 opportunity-cost as a contract pass (explicit best-forgone-alternative statement + sunk-cost strip + resource-discipline check) on any scarce-resource-fork + sunk-spend signature; pair with m044 stakeholder when an interested party champions sunk work; report the opportunity-cost gap as a packet line (the $1.5M is the price of 'finishing what we started') so the verdict IS the comparison — this converts the v5 REVIEW-repair into in-frame reasoning and flips the POS-01 loss."
  },
  {
    "test_case_id": "m023-NEG-01",
    "human_model": "Opportunity Cost Thinking",
    "winner": "ai",
    "key_gap": "vs baseline: the human baseline applied the style purely and failed on cue — exhaustive forgone-alternative enumeration with no stop condition cycled re-pricing passes past all three 14-day windows (all offers forgone, counteroffer withdrawn) and monetized the non-fungible. The non-routed v5 AI already won (2.0/5.0) by improvising the band -> VOI~0 -> rule chain, but the routed run makes the escape structural: m088 pre-commitment leads (the re-analysis loop is the temptation the pass pre-binds against; the 48h commit is a Ulysses contract), m023 runs as protective context with its known trap gate-checked (its own Step-7 insight — 'the best forgone alternative is the decision itself' — operationalized in-frame, not discovered at REVIEW), m031 bounds the analysis to one discriminating pass with a falsifier, and tempo mode (P2) commits at DO.",
    "learning_signal": "On near-tie signatures with hard windows, the opportunity-cost discipline must be applied against itself: when the error band makes ranking differences noise, the costliest forgone alternative is the decision itself, so the stop condition (VOI ~ 0) and the commitment mechanism (pre-stated tie-breaker + binding deadline) ARE the deliverable, not another decimal place; the registry's own weakness ('counting everything can freeze action') is the routing flag that a protective gate (R2) must pair with the style — on NEG signatures the router should move top-1 away from the trap style toward a commitment mechanism (m088), holding the negative split at >= 90% AI wins.",
    "suggested_improvement": "Keep m088 pre-commitment as the primary route for hard-window near-ties (the loop is a temptation, not a computation), with m023 as gate-checked context rather than lead; install 'time-box enumeration to one pass with explicit error bands; commit inside the window on a pre-stated tie-breaker' as a standing contract; log every near-tie where re-pricing cycled as a curriculum item — tempo mode (P2) already enforces the commit at DO, so the loop-catastrophe is now structurally unreachable."
  }
]
