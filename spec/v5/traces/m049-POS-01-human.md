# Human Baseline — m049-POS-01
## Style: First Principles in Business Strategy (pure) — visible reasoning trace

**Step 1 — Discard the playbook as evidence.** Benchmarks are descriptions of other companies' physics, not arguments. The board's numbers (CAC $150, spend 30–35% of revenue, peer churn 6–8%) describe public comparables; they tell me nothing about TerraPet. Derive from TerraPet's own numbers: ARPU $60, gross margin 65% → $39/month contribution; churn 20%/month.

**Step 2 — Derive the retention physics.** Lifetime = 1/churn = 1/0.20 = 5 months. LTV = contribution × lifetime = $39 × 5 = $195. The cohort curve adds the decisive fact: 65% of churn sits in months 1–3 and is invariant across acquisition channels — the customer's first three months are the physics; the channel that brought them is not.

**Step 3 — Run the board's plan through TerraPet's physics.** Per acquired customer: $195 LTV − $150 CAC − $18 first-order promo = +$27 before fixed overhead (onboarding/support ≈ $15–20 per customer). The board's plan nets ≈ $0 per customer and the 30% discount depresses month-1 revenue further. Scaling acquisition multiplies ≈ zero. The peer math that makes this playbook work — LTV/CAC ≈ 3.7, ≈ +$400 per customer — is a property of the peers' churn (7% → lifetime ≈ 14 months → LTV ≈ $557). Same CAC, different retention, different business.

**Step 4 — Locate the binding constraint.** Not acquisition — the channel split shows churn identical across channels, and 65% of the loss happens inside months 1–3. The binding constraint is retention physics at the start of the customer life. Spending on acquisition before fixing that is spending to pour water into a leaking first bucket.

**Step 5 — Derive the acquisition rule from LTV, not from peers.** Breakeven CAC pre-fix = LTV = $195 (nothing left for overhead, so the ceiling must be lower in practice). Post-fix, churn 8% → lifetime 12.5 months → LTV ≈ $487; a 4:1 LTV/CAC target → CAC ceiling ≈ $120. Promotion eliminated (−$18 per acquisition). Retention first: ≈ $400k program aimed at months 1–3; scale acquisition only after the churn gate is measured.

**Step 6 — Quantify and verify.** Post-fix customer contribution ≈ $487 − $120 = $368 vs ≈ $27 today — a 13× improvement in the unit that gets multiplied. 3× subscriber growth only after churn < 12% is measured in cohorts. Falsifier: if months-1–3 churn has not fallen below 12% within three quarters, the retention thesis fails and the acquisition spend stays capped.

**Trace summary box.** Own physics: churn 20% → lifetime 5 mo → LTV $195 → board plan nets ≈ $0/customer (CAC $150 + promo $18) · benchmark's +$400 is the peers' churn, not ours · binding constraint = months-1–3 retention · derived rule: retention program ≈ $400k → churn 8% → LTV $487 → CAC ≤ $120, no promo → ≈ $368/customer; scale only past the churn gate.

*Baseline integrity note (grader metadata, not part of the reasoning): reasoning confined to pure First Principles in Business Strategy — unit economics and customer fundamentals derived from TerraPet's own cohort physics; the playbook is used only as the target of rejection, never as a premise. Signature moves: LTV from 1/churn, CAC ceiling derived from LTV and a margin target, binding constraint located inside the cohort curve.*
