# Human Baseline — m053-POS-01
## Style: Root Cause Analysis in Incident Response (pure) — visible reasoning trace

**Step 1 — Timeline first, evidence on every node.** 18:00 C-4417 auto-promoted (idempotency window 5 min → 30 s); 18:05–20:57 FTR 0.1% → 2.4%, gradual — matches a retry cadence (client retries at 30 s) colliding with the new window, not a crash or a spike; 20:58 page, which at the August threshold (1%) should have fired ≈ 18:11; 21:04–21:46 DB on-call chasing lock-wait spikes; 21:46 rollback; 21:58 baseline. Deploy log, metric ramp, alert-config diff, reorg roster, rollback log — every node has a receipt. Timeline before hypotheses: the ramp's *gradualness* is itself evidence.

**Step 2 — Two why-chains, not one.** Chain A (failure): failed transactions → duplicate submissions now rejected (30 s window == 30 s retry) → C-4417 changed the window → why did it ship? auto-promotion classified "config" as low-risk → no approval, no canary. **Root is the pipeline heuristic, not the config.** Chain B (detection): page at 20:58 instead of 18:11 → threshold at 5% → raised in August to cut noise → an alert-budget trade made with no delta-from-baseline compensation; 42-min MTTR → page routed to the DB rotation → reorg reassigned ownership and alert routing was never migrated. Two independent detection failures; each is its own gap.

**Step 3 — Trigger / root / amplifier separation.** Trigger: C-4417 — reverting it restored service; necessary, not sufficient. Root: the low-risk auto-promotion of config changes (any future config of that class re-ships identically). Amplifier: the 30 s retry storm — it multiplied duplicate load and produced the lock-wait red herring that ate 42 minutes. Amplifiers get documented, not fixed.

**Step 4 — Prevention vs detection split, recurrence test per item.** Prevention: (P1) config-class changes require approval + canary/gradual rollout — catches the class, not just C-4417; (P2) config regression test asserting idempotency semantics survive window changes. Detection: (D1) restore the 1% threshold or alert on delta-from-baseline — would have paged at 18:11, cutting the window by ~35 min; (D2) migrate alert routing to the payments rotation — kills the 42-min MTTR; (D3) duplicate-rate metric — the failure signature itself, not its side effects. Recurrence test: P1 alone prevents the incident (nothing ships); D1 alone cuts it to minutes. Both pass.

**Step 5 — Discipline, and the meta-point.** Five owned items; no culture-ladder whys; no individual named — the August threshold decision is a process gap. And notice: the threshold raise is itself a small config change — **the same class as the trigger**. The fix for this incident and the fix that would have prevented it are the same fix.

**Trace summary box.** Timeline (all links receipted) → two chains (failure + detection) → trigger C-4417 / root pipeline heuristic / amplifier retry storm → prevention P1–P2, detection D1–D3, each with recurrence test → blameless, ≤ 5 items.

*Baseline integrity note (grader metadata, not part of the reasoning): reasoning confined to pure Root Cause Analysis in Incident Response: timeline-first with evidence per node, why-chains to mechanism, trigger/root/amplifier separation, explicit prevention vs detection gap split, recurrence test per action item, blameless framing. Signature moves: the two-chain (failure + detection) walk and the "threshold raise is the same class as the trigger" meta-find.*
