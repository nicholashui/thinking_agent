# Human Baseline — m068-NEG-01
## Style: Hypothesis-Driven Consulting (MECE) (pure) — visible reasoning trace

**Step 1 — Build the tree for Category A.** Margin = Price − COGS − Freight − Returns. Decompose the 19-point decline branch by branch: realized price −0.3 (flat at category level), volume/SKU mix −0.2 (stable), COGS +0.1 (contracts flat), freight +1.0, returns flat. The branches explain ≈ 1 point of the decline.

**Step 2 — Reconcile the residual.** ~18 points are unexplained by any branch. The finance note supplies the answer: the Category A rebates accrual was restated in Q3 — a one-time accounting timing item. That is measurement noise, not a driver. Attribution is a discipline: the tree reports what the branches show, and the residual goes to the restatement line, not to a new branch invented to make the numbers prettier.

**Step 3 — Prioritize by impact × controllability.** Freight is the only branch with real movement we can act on (+1.0). The contract memo is noted and pruned: the top account is ≈ 8% of category revenue — even a 20-point margin swing there moves category margin by ≈ 1.6 points, roughly 8% of the decline. Immaterial at the category level. That is exactly what the discipline is for: impact is measured on the branch, not on the anecdote.

**Step 4 — State the hypothesis to test first.** H1: freight cost inflation (new lanes, fuel) is the actionable driver of the explained decline. Test: lane-level cost-per-order benchmark vs market rates; pilot surcharge pass-through on two lanes. H2 (backup): COGS contract renegotiation at the next annual window.

**Step 5 — Recommendation and decision.** Cut freight ≈ 1pt via lane renegotiation + surcharge pass-through; renegotiate COGS at contract window; monitor the top account's program for two quarters and revisit only if margin recovery misses forecast by > 1pt. Client-ready page: "Category A −19pts: freight is the actionable driver; ~18pts traces to the Q3 rebates restatement. Action: lane renegotiation, surcharge pass-through."

**Trace summary box.** branch attribution ≈ −1pt → residual ~18pts → reconciled to Q3 restatement (noise, not a driver) → freight = top prioritized branch → H1 = freight inflation, lane test → account program pruned as immaterial-to-averages → cost-cutting recommendation.

*Baseline integrity note (grader metadata, not part of the reasoning): reasoning confined to pure Hypothesis-Driven Consulting (MECE) — and the style's documented weaknesses ("tree too rigid, premature pruning") are exactly what is exercised: the tree is built from the supplied branches, the residual is force-fit to the finance note's restatement, and the account-level anomaly is pruned as immaterial to category averages while the true driver (the renegotiated program — a missing "terms" branch) is left untouched. This is the informative failure.*
